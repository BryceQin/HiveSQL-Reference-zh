<!-- TOC  -->
- [1. 数据库操作（Database Operations）](#1-数据库操作database-operations)
    - [1.1 CREATE DATABASE](#11-create-database)
    - [1.2 DROP DATABASE](#12-drop-database)
    - [1.3 ALTER DATABASE](#13-alter-database)
    - [1.4 USE DATABASE](#14-use-database)
    - [1.5 SHOW DATABASES](#15-show-databases)
    - [1.6 DESCRIBE DATABASE](#16-describe-database)
- [2. 创建表（CREATE TABLE）](#2-创建表create-table)
    - [2.1 管理表（Managed Table）](#21-管理表managed-table)
    - [2.2 外部表（External Table）](#22-外部表external-table)
    - [2.3 管理表 vs 外部表对比](#23-管理表-vs-外部表对比)
    - [2.4 CREATE TABLE ... LIKE](#24-create-table--like)
    - [2.5 CREATE TABLE ... AS SELECT（CTAS）](#25-create-table--as-selectctas)
    - [2.6 临时表（TEMPORARY TABLE）](#26-临时表temporary-table)
    - [2.7 事务表（Transactional Table / ACID）](#27-事务表transactional-table--acid)
- [3. 删除表（DROP TABLE）](#3-删除表drop-table)
    - [3.1 DROP TABLE](#31-drop-table)
    - [3.2 DROP TABLE IF EXISTS](#32-drop-table-if-exists)
    - [3.3 PURGE 跳过回收站](#33-purge-跳过回收站)
- [4. 修改表（ALTER TABLE）](#4-修改表alter-table)
    - [4.1 RENAME TO（重命名表）](#41-rename-to重命名表)
    - [4.2 ADD COLUMNS（添加列）](#42-add-columns添加列)
    - [4.3 CHANGE COLUMN（修改列）](#43-change-column修改列)
    - [4.4 REPLACE COLUMNS（替换所有列）](#44-replace-columns替换所有列)
    - [4.5 SET TBLPROPERTIES（设置表属性）](#45-set-tblproperties设置表属性)
    - [4.6 其他 SET 操作](#46-其他-set-操作)
- [5. 分区表操作（Partition Operations）](#5-分区表操作partition-operations)
    - [5.1 ADD PARTITION（添加分区）](#51-add-partition添加分区)
    - [5.2 DROP PARTITION（删除分区）](#52-drop-partition删除分区)
    - [5.3 RENAME PARTITION（重命名分区）](#53-rename-partition重命名分区)
    - [5.4 MSCK REPAIR TABLE（修复分区）](#54-msck-repair-table修复分区)
    - [5.5 SHOW PARTITIONS（查看分区）](#55-show-partitions查看分区)
- [6. 视图（Views）](#6-视图views)
    - [6.1 CREATE VIEW](#61-create-view)
    - [6.2 DROP VIEW](#62-drop-view)
    - [6.3 ALTER VIEW](#63-alter-view)
    - [6.4 SHOW VIEWS](#64-show-views)
- [7. 查看信息命令（Information Commands）](#7-查看信息命令information-commands)
    - [7.1 SHOW TABLES](#71-show-tables)
    - [7.2 SHOW CREATE TABLE](#72-show-create-table)
    - [7.3 DESCRIBE / DESC](#73-describe--desc)
    - [7.4 DESCRIBE FORMATTED](#74-describe-formatted)
    - [7.5 DESCRIBE EXTENDED](#75-describe-extended)
    - [7.6 SHOW COLUMNS](#76-show-columns)
    - [7.7 SHOW FUNCTIONS](#77-show-functions)
    - [7.8 SHOW TBLPROPERTIES](#78-show-tblproperties)
    - [7.9 SHOW LOCKS](#79-show-locks)
<!-- /TOC -->
# 1. 数据库操作（Database Operations）
## 1.1 CREATE DATABASE
```sql
语法：
    CREATE DATABASE|SCHEMA [IF NOT EXISTS] <database_name>
    [COMMENT <database_comment>]
    [LOCATION <hdfs_path>]
    [MANAGEDLOCATION <hdfs_path>]
    [WITH DBPROPERTIES (<property_name>=<property_value> [, ...])];
说明：
    创建数据库。DATABASE 与 SCHEMA 等效。
    可通过 COMMENT 添加描述，LOCATION 指定 HDFS 存储路径，DBPROPERTIES 设置自定义属性。
举例：
    -- 创建基本数据库
    CREATE DATABASE IF NOT EXISTS my_db
    COMMENT '我的数据库';

    -- 指定存储路径和属性
    CREATE DATABASE analytics_db
    COMMENT '数据分析库'
    LOCATION '/warehouse/analytics'
    WITH DBPROPERTIES ('created_by'='admin', 'created_date'='2024-01-01');
```

## 1.2 DROP DATABASE
```sql
语法：
    DROP DATABASE|SCHEMA [IF EXISTS] <database_name> [RESTRICT|CASCADE];
说明：
    RESTRICT（默认）：数据库非空时不允许删除，防止误删。
    CASCADE：强制删除数据库及其下所有表，谨慎使用。
举例：
    -- 安全删除空数据库
    DROP DATABASE IF EXISTS my_db;

    -- 删除空数据库（非空时报错）
    DROP DATABASE my_db RESTRICT;

    -- 级联删除数据库及所有表
    DROP DATABASE analytics_db CASCADE;
```

## 1.3 ALTER DATABASE
```sql
语法：
    ALTER DATABASE|SCHEMA <database_name> SET DBPROPERTIES (<property_name>=<property_value>, ...);
    ALTER DATABASE|SCHEMA <database_name> SET OWNER [USER|ROLE] <user_or_role>;
    ALTER DATABASE|SCHEMA <database_name> SET LOCATION <hdfs_path>;
    ALTER DATABASE|SCHEMA <database_name> SET MANAGEDLOCATION <hdfs_path>;
说明：
    修改数据库属性、所有者、存储路径等元数据。不支持修改数据库名称。
举例：
    ALTER DATABASE my_db SET DBPROPERTIES ('updated_by'='admin', 'update_date'='2024-06-01');

    ALTER DATABASE my_db SET OWNER USER new_owner;

    ALTER DATABASE analytics_db SET LOCATION '/warehouse/analytics_v2';
```

## 1.4 USE DATABASE
```sql
语法：
    USE <database_name>;
说明：
    切换当前会话的默认数据库。后续所有操作默认在此数据库下执行。
举例：
    USE my_db;

    -- 验证当前数据库
    SELECT CURRENT_DATABASE();
    -- 结果：my_db

    -- 切换到默认数据库
    USE DEFAULT;
```

## 1.5 SHOW DATABASES
```sql
语法：
    SHOW DATABASES|SCHEMAS [LIKE '<pattern>'];
说明：
    列出所有数据库。支持 LIKE 模糊匹配。
举例：
    -- 查看所有数据库
    SHOW DATABASES;
    -- 结果：
    -- default
    -- my_db
    -- analytics_db

    -- 模糊匹配
    SHOW DATABASES LIKE '*analytics*';
```

## 1.6 DESCRIBE DATABASE
```sql
语法：
    DESCRIBE|DESC DATABASE|SCHEMA [EXTENDED] <database_name>;
说明：
    查看数据库的描述信息。加上 EXTENDED 可显示 DBPROPERTIES 和 LOCATION 等详细信息。
举例：
    -- 基本信息
    DESCRIBE DATABASE my_db;
    -- 结果：
    -- my_db 我的数据库  hdfs://.../warehouse/my_db.db  hadoop  USER

    -- 扩展信息（含属性）
    DESCRIBE DATABASE EXTENDED analytics_db;
    -- 结果：包含 LOCATION、DBPROPERTIES 等完整信息
```

# 2. 创建表（CREATE TABLE）
## 2.1 管理表（Managed Table）
```sql
语法：
    CREATE TABLE [IF NOT EXISTS] <table_name> (
        <col_name> <data_type> [COMMENT <col_comment>],
        ...
    )
    [COMMENT '<table_comment>']
    [PARTITIONED BY (<col_name> <data_type>, ...)]
    [CLUSTERED BY (<col_name>, ...) [SORTED BY (<col_name> [ASC|DESC], ...)] INTO <n> BUCKETS]
    [ROW FORMAT <row_format>]
    [STORED AS <file_format>]
    [LOCATION '<hdfs_path>']
    [TBLPROPERTIES ('<key>'='<value>', ...)];
说明：
    管理表（内部表）：Hive 完全管理其生命周期。DROP TABLE 会同时删除元数据和 HDFS 数据文件。
    如果未指定 LOCATION，数据存储在 hive.metastore.warehouse.dir 下。
举例：
    CREATE TABLE IF NOT EXISTS employees (
        id INT COMMENT '员工ID',
        name STRING COMMENT '姓名',
        salary DECIMAL(10, 2) COMMENT '薪资',
        hire_date DATE COMMENT '入职日期'
    )
    COMMENT '员工信息表'
    PARTITIONED BY (dept STRING COMMENT '部门')
    ROW FORMAT DELIMITED
    FIELDS TERMINATED BY ','
    STORED AS TEXTFILE
    TBLPROPERTIES ('created_by'='hr_team');
```

## 2.2 外部表（External Table）
```sql
语法：
    CREATE EXTERNAL TABLE [IF NOT EXISTS] <table_name> (
        <col_name> <data_type> [COMMENT <col_comment>],
        ...
    )
    [COMMENT '<table_comment>']
    [PARTITIONED BY (...)]
    [ROW FORMAT <row_format>]
    [STORED AS <file_format>]
    LOCATION '<hdfs_path>'
    [TBLPROPERTIES (...)];
说明：
    外部表：Hive 只管理元数据，不管理数据文件。通过 EXTERNAL 关键字和 LOCATION 指定外部路径。
    DROP TABLE 仅删除元数据，HDFS 数据文件保留。
    适用于已有 HDFS 数据需要被 Hive 查询，或需要跨多个工具共享数据的场景。
举例：
    CREATE EXTERNAL TABLE IF NOT EXISTS click_logs (
        user_id STRING COMMENT '用户ID',
        event STRING COMMENT '事件类型',
        event_time TIMESTAMP COMMENT '事件时间'
    )
    COMMENT '点击日志外部表'
    PARTITIONED BY (dt STRING COMMENT '日期分区')
    ROW FORMAT DELIMITED
    FIELDS TERMINATED BY '\t'
    STORED AS TEXTFILE
    LOCATION '/data/logs/clicks'
    TBLPROPERTIES ('external.table.purge'='true');
```

## 2.3 管理表 vs 外部表对比
```sql
-- ========== 管理表 ==========
CREATE TABLE managed_test (id INT, name STRING);
INSERT INTO managed_test VALUES (1, 'test');

-- 删除：元数据和数据文件都会被删除
DROP TABLE managed_test;
-- 结果：表和数据均被删除，无法恢复

-- ========== 外部表 ==========
CREATE EXTERNAL TABLE external_test (id INT, name STRING)
LOCATION '/tmp/external_test';
INSERT INTO external_test VALUES (1, 'test');

-- 删除：仅删除元数据，数据文件保留在 HDFS
DROP TABLE external_test;
-- 结果：表定义被删除，但 /tmp/external_test 下的数据文件依然存在

-- 之后可重新基于原有数据创建外部表
CREATE EXTERNAL TABLE external_test (id INT, name STRING)
LOCATION '/tmp/external_test';

-- ========== 关键区别 ==========
/*
| 对比项       | 管理表（内部表）        | 外部表（External Table）   |
|-------------|------------------------|---------------------------|
| 关键字       | 无（默认）              | EXTERNAL                  |
| LOCATION    | 可选，默认仓库目录        | 必须或强烈建议指定          |
| DROP 行为    | 删除元数据 + HDFS 数据   | 仅删除元数据，数据保留       |
| 数据安全性    | Hive 独占管理            | 多工具共享，更安全          |
| 适用场景     | ETL 中间结果、临时表     | 原始数据、共享数据源         |
*/
```

## 2.4 CREATE TABLE ... LIKE
```sql
语法：
    CREATE TABLE [IF NOT EXISTS] <new_table> LIKE <existing_table>;
说明：
    仅复制表结构（列定义、分区、SerDe、存储格式等），不复制数据。
举例：
    -- 基于 employees 表结构创建空表
    CREATE TABLE employees_backup LIKE employees;

    -- 验证：结构一致但无数据
    SELECT COUNT(*) FROM employees_backup;
    -- 结果：0
```

## 2.5 CREATE TABLE ... AS SELECT（CTAS）
```sql
语法：
    CREATE TABLE [IF NOT EXISTS] <new_table>
    [COMMENT '<comment>']
    [ROW FORMAT <row_format>]
    [STORED AS <file_format>]
    [TBLPROPERTIES (...)]
    AS <select_statement>;
说明：
    根据 SELECT 查询结果创建表，同时复制结构和数据。
    CTAS 创建的表默认为管理表，不可用于外部表。
举例：
    CREATE TABLE high_salary_employees
    COMMENT '高薪员工表'
    STORED AS ORC
    AS
    SELECT id, name, salary, dept
    FROM employees
    WHERE salary > 10000;
```

## 2.6 临时表（TEMPORARY TABLE）
```sql
语法：
    CREATE TEMPORARY TABLE <table_name> (
        <col_name> <data_type>, ...
    )
    [STORED AS <file_format>];
说明：
    临时表仅在当前会话内可见，会话结束时自动删除。
    不能创建分区临时表，不支持索引。若与已有永久表同名，临时表优先。
举例：
    -- 创建临时表
    CREATE TEMPORARY TABLE tmp_result (
        id INT,
        metric DOUBLE
    );

    INSERT INTO tmp_result VALUES (1, 99.5), (2, 87.3);

    SELECT * FROM tmp_result;
    -- +-----+---------+
    -- | id  | metric  |
    -- +-----+---------+
    -- | 1   | 99.5    |
    -- | 2   | 87.3    |
    -- +-----+---------+

    -- 与永久表重名时临时表优先
    CREATE TABLE perm_test (id INT);
    CREATE TEMPORARY TABLE perm_test (id STRING);

    DESC perm_test;
    -- 结果：显示 STRING 类型列（临时表的定义）

    -- 退出会话后临时表自动删除
```

## 2.7 事务表（Transactional Table / ACID）
```sql
语法：
    CREATE TABLE <table_name> (
        <col_name> <data_type>, ...
    )
    CLUSTERED BY (<col_name>) INTO <n> BUCKETS
    STORED AS ORC
    TBLPROPERTIES ('transactional'='true');
说明：
    支持 INSERT、UPDATE、DELETE 和 MERGE 操作的事务表。
    必须分桶（bucketed）、必须使用 ORC 格式、必须设置 transactional=true。
举例：
    CREATE TABLE orders_acid (
        order_id BIGINT,
        customer_name STRING,
        amount DECIMAL(10, 2),
        status STRING
    )
    CLUSTERED BY (order_id) INTO 4 BUCKETS
    STORED AS ORC
    TBLPROPERTIES ('transactional'='true');

    -- 事务表支持 UPDATE / DELETE
    INSERT INTO orders_acid VALUES (1, 'Alice', 299.99, 'pending');

    UPDATE orders_acid SET status = 'completed' WHERE order_id = 1;

    DELETE FROM orders_acid WHERE status = 'cancelled';

    -- MERGE 操作（UPSERT）
    MERGE INTO orders_acid AS t
    USING (
        SELECT 1 AS order_id, 'Alice' AS customer_name,
               350.00 AS amount, 'shipped' AS status
    ) AS s
    ON t.order_id = s.order_id
    WHEN MATCHED THEN UPDATE SET amount = s.amount, status = s.status
    WHEN NOT MATCHED THEN INSERT VALUES (s.order_id, s.customer_name, s.amount, s.status);
```

# 3. 删除表（DROP TABLE）
## 3.1 DROP TABLE
```sql
语法：
    DROP TABLE [IF EXISTS] <table_name> [PURGE];
说明：
    管理表：删除元数据 + 删除 HDFS 数据文件。
    外部表：仅删除元数据，HDFS 数据文件保留。
举例：
    DROP TABLE IF EXISTS employees;
```

## 3.2 DROP TABLE IF EXISTS
```sql
说明：
    IF EXISTS 避免表不存在时抛出错误，使脚本更加健壮。
举例：
    -- 无 IF EXISTS：表不存在时报错
    DROP TABLE non_existent_table;
    -- 结果：FAILED: SemanticException Table not found

    -- 有 IF EXISTS：静默跳过
    DROP TABLE IF EXISTS non_existent_table;
    -- 结果：OK（无任何操作）
```

## 3.3 PURGE 跳过回收站
```sql
说明：
    默认情况下 DROP TABLE 会将数据移动到回收站（如果配置了）。加上 PURGE 彻底删除数据，跳过回收站。
举例：
    -- 彻底删除（不经过回收站，不可恢复）
    DROP TABLE employees PURGE;

    -- 管理表 + PURGE：元数据和数据文件都被永久删除
    DROP TABLE employees PURGE;

    -- 外部表 + PURGE：元数据被永久删除，HDFS 数据仍然保留
    -- 除非设置了 external.table.purge=true
    DROP TABLE click_logs PURGE;
```

# 4. 修改表（ALTER TABLE）
## 4.1 RENAME TO（重命名表）
```sql
语法：
    ALTER TABLE <old_table_name> RENAME TO <new_table_name>;
说明：
    重命名表。涉及的分区元数据也会一并更新。
举例：
    ALTER TABLE employees RENAME TO staff;

    SELECT * FROM staff LIMIT 1;
    -- 原 employees 数据全部可通过新名称 staff 查询
```

## 4.2 ADD COLUMNS（添加列）
```sql
语法：
    ALTER TABLE <table_name> ADD COLUMNS (<col_name> <data_type> [COMMENT '<comment>'], ...);
说明：
    向现有表追加新列。新列会添加到末尾（分区列之前）。
    对于已有数据的分区，新列的值为 NULL。
举例：
    -- 添加单个列
    ALTER TABLE staff ADD COLUMNS (email STRING COMMENT '邮箱');

    -- 添加多个列
    ALTER TABLE staff ADD COLUMNS (
        phone STRING COMMENT '电话',
        address STRING COMMENT '地址'
    );

    -- 查看修改后的表结构
    DESC staff;
    -- 结果：id, name, salary, hire_date, email, phone, address + 分区列 dept
```

## 4.3 CHANGE COLUMN（修改列）
```sql
语法：
    ALTER TABLE <table_name> CHANGE [COLUMN] <old_col_name> <new_col_name> <data_type>
    [COMMENT '<comment>'] [FIRST|AFTER <col_name>];
说明：
    可同时修改列名、数据类型、注释、位置。只会修改元数据，不会改变实际数据。
    若修改类型，需确保旧数据能兼容新类型。
    FIRST / AFTER 可调整列顺序，仅影响元数据展示。
举例：
    -- 只修改列名
    ALTER TABLE staff CHANGE COLUMN name full_name STRING COMMENT '全名';

    -- 修改类型和注释
    ALTER TABLE staff CHANGE COLUMN salary salary DECIMAL(12, 2) COMMENT '薪资（保留两位小数）';

    -- 修改列名并调整位置到第一列
    ALTER TABLE staff CHANGE COLUMN id employee_id INT COMMENT '员工ID' FIRST;

    -- 调整列顺序
    ALTER TABLE staff CHANGE COLUMN email email STRING COMMENT '邮箱' AFTER full_name;
```

## 4.4 REPLACE COLUMNS（替换所有列）
```sql
语法：
    ALTER TABLE <table_name> REPLACE COLUMNS (<col_name> <data_type> [COMMENT '<comment>'], ...);
说明：
    用新的列定义完全替换现有列，旧列全部移除。仅修改元数据。
    数据文件不变，新列定义会按顺序映射到 HDFS 中的文件列。
举例：
    -- 假设原表列：id, name, salary, hire_date, dept(分区)
    ALTER TABLE staff REPLACE COLUMNS (
        emp_id BIGINT COMMENT '员工编号',
        emp_name STRING COMMENT '员工姓名',
        department STRING COMMENT '部门'
    );

    DESC staff;
    -- 结果：只有 emp_id, emp_name, department 三列 + dept(分区列)

    -- 注意：REPLACE COLUMNS 不会删除分区列，但非分区列将被完全替换
```

## 4.5 SET TBLPROPERTIES（设置表属性）
```sql
语法：
    ALTER TABLE <table_name> SET TBLPROPERTIES ('<key>'='<value>', ...);
说明：
    设置或修改表的自定义属性。用于配置存储、压缩、事务等行为。
举例：
    -- 设置压缩属性
    ALTER TABLE staff SET TBLPROPERTIES (
        'orc.compress'='SNAPPY',
        'orc.compress.size'='262144'
    );

    -- 修改注释
    ALTER TABLE staff SET TBLPROPERTIES ('comment'='公司员工信息表（更新版）');

    -- 设置外部表删除时同时清理数据
    ALTER TABLE click_logs SET TBLPROPERTIES ('external.table.purge'='true');
```

## 4.6 其他 SET 操作
```sql
语法：
    ALTER TABLE <table_name> SET SERDE '<serde_class>' [WITH SERDEPROPERTIES (...)];
    ALTER TABLE <table_name> SET FILEFORMAT <file_format>;
    ALTER TABLE <table_name> SET LOCATION '<hdfs_path>';
说明：
    SET SERDE：自定义序列化/反序列化器。
    SET FILEFORMAT：更改表的数据存储格式。
    SET LOCATION：更改表的数据存储路径（仅修改元数据，已有数据不会自动迁移）。
举例：
    -- 设置 SerDe
    ALTER TABLE staff SET SERDE 'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe'
    WITH SERDEPROPERTIES ('field.delim'=',');

    -- 更改文件格式
    ALTER TABLE staff SET FILEFORMAT ORC;

    -- 更改存储路径
    ALTER TABLE staff SET LOCATION '/warehouse/hr/staff_v2';
```

# 5. 分区表操作（Partition Operations）
## 5.1 ADD PARTITION（添加分区）
```sql
语法：
    ALTER TABLE <table_name> ADD [IF NOT EXISTS]
    PARTITION (<part_col>=<value>, ...) [LOCATION '<path>']
    [PARTITION (<part_col>=<value>, ...) [LOCATION '<path>'] ...];
说明：
    手动添加分区。可同时添加多个分区，每个分区可指定独立的 LOCATION。
    IF NOT EXISTS 避免分区已存在时报错。
举例：
    ALTER TABLE click_logs ADD IF NOT EXISTS
    PARTITION (dt='2024-01-01') LOCATION '/data/logs/clicks/dt=2024-01-01'
    PARTITION (dt='2024-01-02') LOCATION '/data/logs/clicks/dt=2024-01-02'
    PARTITION (dt='2024-01-03') LOCATION '/data/logs/clicks/dt=2024-01-03';

    -- 添加分区但不指定 LOCATION（使用默认路径）
    ALTER TABLE staff ADD PARTITION (dept='engineering');
```

## 5.2 DROP PARTITION（删除分区）
```sql
语法：
    ALTER TABLE <table_name> DROP [IF EXISTS] PARTITION (<part_col>=<value>, ...) [PURGE];
说明：
    删除指定分区。管理表的该分区的数据会被删除，外部表仅删除分区元数据。
    加 PURGE 跳过回收站，直接永久删除。
举例：
    -- 删除单个分区
    ALTER TABLE click_logs DROP IF EXISTS PARTITION (dt='2024-01-01');

    -- 同时删除多个分区
    ALTER TABLE click_logs DROP
    PARTITION (dt='2024-01-02'),
    PARTITION (dt='2024-01-03');

    -- 彻底删除（PURGE）
    ALTER TABLE click_logs DROP PARTITION (dt='2024-01-01') PURGE;

    -- 通过条件批量删除分区（动态分区删除）
    -- 注意：需配置 hive.exec.dynamic.partition.mode=nonstrict
    ALTER TABLE click_logs DROP PARTITION (dt <= '2023-12-31');
```

## 5.3 RENAME PARTITION（重命名分区）
```sql
语法：
    ALTER TABLE <table_name> PARTITION (<part_col>=<old_value>) RENAME TO PARTITION (<part_col>=<new_value>);
说明：
    重命名分区值。仅修改元数据，HDFS 数据文件不会自动重命名。
举例：
    ALTER TABLE click_logs PARTITION (dt='2024-01-01') RENAME TO PARTITION (dt='20240101');
```

## 5.4 MSCK REPAIR TABLE（修复分区）
```sql
语法：
    MSCK [REPAIR] TABLE <table_name> [ADD|DROP|SYNC PARTITIONS];
说明：
    当 HDFS 上已有按分区目录结构存放的数据时，MSCK 可自动扫描并修复分区元数据。
    ADD PARTITIONS：将 HDFS 中存在但 Metastore 中缺失的分区添加进来。
    DROP PARTITIONS：删除 Metastore 中存在但 HDFS 中目录不存在的分区。
    SYNC PARTITIONS：等同于 ADD + DROP。
举例：
    -- 假设 HDFS 中已有 /data/logs/clicks/dt=2024-01-01 等目录

    MSCK REPAIR TABLE click_logs;
    -- 结果：自动发现并添加 dt=2024-01-01, dt=2024-01-02... 等分区

    -- 同步分区（添加新分区，删除过期分区）
    MSCK REPAIR TABLE click_logs SYNC PARTITIONS;
```

## 5.5 SHOW PARTITIONS（查看分区）
```sql
语法：
    SHOW PARTITIONS <table_name> [PARTITION (<spec>)];
说明：
    列出表的所有分区，支持按分区条件过滤。
举例：
    -- 查看所有分区
    SHOW PARTITIONS click_logs;
    -- 结果：
    -- dt=2024-01-01
    -- dt=2024-01-02
    -- dt=2024-01-03

    -- 按分区条件过滤
    SHOW PARTITIONS click_logs PARTITION (dt >= '2024-01-02');
```

# 6. 视图（Views）
## 6.1 CREATE VIEW
```sql
语法：
    CREATE VIEW [IF NOT EXISTS] <view_name> [ (<col_name> [COMMENT '<comment>'], ...) ]
    [COMMENT '<view_comment>']
    [TBLPROPERTIES ('<key>'='<value>', ...)]
    AS <select_statement>;
说明：
    创建视图，封装复杂查询逻辑。视图不存储数据，仅存储查询定义。
    视图是只读的，不支持 INSERT / LOAD 操作。
举例：
    CREATE VIEW high_value_orders AS
    SELECT order_id, customer_name, amount, status
    FROM orders_acid
    WHERE amount > 500;

    -- 使用视图查询
    SELECT * FROM high_value_orders;

    -- 带注释和列别名的视图
    CREATE VIEW dept_summary
    COMMENT '部门薪资汇总视图'
    AS
    SELECT dept, COUNT(*) AS emp_count, AVG(salary) AS avg_salary
    FROM staff
    GROUP BY dept;
```

## 6.2 DROP VIEW
```sql
语法：
    DROP VIEW [IF EXISTS] <view_name>;
说明：
    删除视图。仅删除元数据，不影响底层表数据。
举例：
    DROP VIEW IF EXISTS high_value_orders;
```

## 6.3 ALTER VIEW
```sql
语法：
    ALTER VIEW <view_name> AS <new_select_statement>;
    ALTER VIEW <view_name> SET TBLPROPERTIES ('<key>'='<value>', ...);
说明：
    修改视图的定义或属性。视图名不可修改（需 DROP + CREATE 实现）。
举例：
    -- 修改视图查询逻辑
    ALTER VIEW high_value_orders AS
    SELECT order_id, customer_name, amount, status
    FROM orders_acid
    WHERE amount > 1000;

    -- 修改视图属性
    ALTER VIEW dept_summary SET TBLPROPERTIES ('comment'='部门薪资汇总视图（阈值>10000）');
```

## 6.4 SHOW VIEWS
```sql
语法：
    SHOW VIEWS [IN|FROM <database_name>] [LIKE '<pattern>'];
说明：
    列出数据库中的所有视图。支持按数据库和模糊匹配筛选。
举例：
    -- 查看当前数据库的视图
    SHOW VIEWS;

    -- 指定数据库
    SHOW VIEWS IN analytics_db;

    -- 模糊匹配
    SHOW VIEWS LIKE '*order*';
```

# 7. 查看信息命令（Information Commands）
## 7.1 SHOW TABLES
```sql
语法：
    SHOW TABLES [IN|FROM <database_name>] [LIKE '<pattern>'];
说明：
    列出数据库中的所有表（不包含视图，视图用 SHOW VIEWS）。
举例：
    SHOW TABLES;

    SHOW TABLES IN analytics_db LIKE '*order*';
```

## 7.2 SHOW CREATE TABLE
```sql
语法：
    SHOW CREATE TABLE <table_name>;
说明：
    显示创建该表时的完整 CREATE TABLE 语句。
举例：
    SHOW CREATE TABLE staff;
    -- 结果：输出完整的 CREATE TABLE 语句，含所有列、分区、存储格式等
```

## 7.3 DESCRIBE / DESC
```sql
语法：
    DESCRIBE|DESC [DATABASE|SCHEMA|TABLE] <name>;
说明：
    查看表的基本结构：列名、数据类型、注释。
举例：
    DESC staff;
    -- 结果：
    -- +------------------+----------------+----------+
    -- | col_name         | data_type      | comment  |
    -- +------------------+----------------+----------+
    -- | emp_id           | int            | 员工ID   |
    -- | full_name        | string         | 全名     |
    -- | salary           | decimal(12,2)  | 薪资     |
    -- | dept             | string         | 部门     |
    -- +------------------+----------------+----------+

    -- 查看指定分区
    DESC staff PARTITION (dept='engineering');

    -- 查看单列
    DESC staff full_name;
```

## 7.4 DESCRIBE FORMATTED
```sql
语法：
    DESCRIBE FORMATTED <table_name>;
说明：
    显示表的完整详细信息，包含：
    - 表类型（MANAGED_TABLE 或 EXTERNAL_TABLE）
    - 存储格式（InputFormat / OutputFormat）
    - 存储位置（Location）
    - SerDe 信息
    - 分区信息
    - 表属性
    - 创建时间等
举例：
    DESCRIBE FORMATTED staff;
    -- 结果（部分）：
    -- # Detailed Table Information
    -- Database:           default
    -- Table Type:         MANAGED_TABLE
    -- Location:           hdfs://.../warehouse/staff
    -- InputFormat:        org.apache.hadoop.hive.ql.io.orc.OrcInputFormat
    -- OutputFormat:       org.apache.hadoop.hive.ql.io.orc.OrcOutputFormat
    -- SerDe Library:      org.apache.hadoop.hive.ql.io.orc.OrcSerde
    -- ...
```

## 7.5 DESCRIBE EXTENDED
```sql
语法：
    DESCRIBE EXTENDED <table_name>;
说明：
    与 FORMATTED 类似，但输出格式为键值对形式，信息更原始，不易读。
    一般推荐使用 FORMATTED。
举例：
    DESCRIBE EXTENDED staff;
    -- 详细元数据以 key=value 格式输出
```

## 7.6 SHOW COLUMNS
```sql
语法：
    SHOW COLUMNS IN|FROM <table_name>;
说明：
    简洁地列出表中的所有列及数据类型。
举例：
    SHOW COLUMNS IN staff;
    -- 结果：
    -- +------------------+
    -- | column           |
    -- +------------------+
    -- | emp_id           |
    -- | full_name        |
    -- | salary           |
    -- +------------------+
```

## 7.7 SHOW FUNCTIONS
```sql
语法：
    SHOW FUNCTIONS [LIKE '<pattern>'];
说明：
    列出所有可用的内置函数和 UDF。支持 LIKE 模糊匹配。
举例：
    -- 列出所有函数
    SHOW FUNCTIONS;

    -- 模糊匹配字符串函数
    SHOW FUNCTIONS LIKE '*substr*';

    -- 查看函数帮助
    DESCRIBE FUNCTION substr;
    DESCRIBE FUNCTION EXTENDED substr;
    -- EXTENDED 会显示函数详细用法和示例
```

## 7.8 SHOW TBLPROPERTIES
```sql
语法：
    SHOW TBLPROPERTIES <table_name> [('<key>')];
说明：
    显示表的 TBLPROPERTIES 属性。指定 key 可查看单个属性。
举例：
    -- 查看所有属性
    SHOW TBLPROPERTIES staff;

    -- 查看单个属性
    SHOW TBLPROPERTIES staff ('transactional');
```

## 7.9 SHOW LOCKS
```sql
语法：
    SHOW LOCKS <table_name> [PARTITION (<part_spec>)] [EXTENDED];
说明：
    显示表或分区的锁信息。用于排查并发写入时的锁冲突。
举例：
    SHOW LOCKS orders_acid;

    SHOW LOCKS orders_acid PARTITION (dt='2024-01-01');

    SHOW LOCKS orders_acid EXTENDED;
    -- EXTENDED 显示更多锁的细节信息
```