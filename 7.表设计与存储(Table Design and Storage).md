<!-- TOC  -->
- [1. 管理表 vs 外部表 (Managed vs External Tables)](#1-管理表-vs-外部表-managed-vs-external-tables)
    - [1.1 概念对比](#11-概念对比)
    - [1.2 创建语法对比](#12-创建语法对比)
    - [1.3 适用场景分析](#13-适用场景分析)
    - [1.4 管理表与外部表互转](#14-管理表与外部表互转)
- [2. 分区表 (Partitioned Tables)](#2-分区表-partitioned-tables)
    - [2.1 分区概念和作用](#21-分区概念和作用)
    - [2.2 单级分区](#22-单级分区)
    - [2.3 多级分区](#23-多级分区)
    - [2.4 静态分区与动态分区](#24-静态分区与动态分区)
    - [2.5 分区裁剪](#25-分区裁剪)
    - [2.6 分区设计最佳实践](#26-分区设计最佳实践)
- [3. 分桶表 (Bucketed / Clustered Tables)](#3-分桶表-bucketed--clustered-tables)
    - [3.1 分桶概念和作用](#31-分桶概念和作用)
    - [3.2 CLUSTERED BY 语法](#32-clustered-by-语法)
    - [3.3 分桶与 SORTED BY](#33-分桶与-sorted-by)
    - [3.4 分桶与 JOIN 优化 (SMB Join)](#34-分桶与-join-优化-smb-join)
    - [3.5 分桶抽样 TABLESAMPLE](#35-分桶抽样-tablesample)
    - [3.6 分桶设计最佳实践](#36-分桶设计最佳实践)
- [4. 文件格式 (File Formats / Storage Formats)](#4-文件格式-file-formats--storage-formats)
    - [4.1 STORED AS 子句](#41-stored-as-子句)
    - [4.2 TEXTFILE](#42-textfile)
    - [4.3 SEQUENCEFILE](#43-sequencefile)
    - [4.4 ORC](#44-orc)
    - [4.5 PARQUET](#45-parquet)
    - [4.6 AVRO](#46-avro)
    - [4.7 RCFILE](#47-rcfile)
    - [4.8 各格式对比与推荐](#48-各格式对比与推荐)
- [5. 压缩 (Compression)](#5-压缩-compression)
    - [5.1 压缩配置](#51-压缩配置)
    - [5.2 常用压缩编码](#52-常用压缩编码)
    - [5.3 中间结果压缩 vs 最终输出压缩](#53-中间结果压缩-vs-最终输出压缩)
    - [5.4 各格式默认压缩](#54-各格式默认压缩)
- [6. SerDe (序列化/反序列化)](#6-serde-序列化反序列化)
    - [6.1 SerDe 概念](#61-serde-概念)
    - [6.2 ROW FORMAT SERDE 语法](#62-row-format-serde-语法)
    - [6.3 常用 SerDe](#63-常用-serde)
    - [6.4 自定义分隔符](#64-自定义分隔符)
    - [6.5 COLLECTION ITEMS 与 MAP KEYS](#65-collection-items-与-map-keys)
- [7. 表属性 (Table Properties)](#7-表属性-table-properties)
    - [7.1 TBLPROPERTIES 常用配置](#71-tblproperties-常用配置)
    - [7.2 ORC/Parquet 相关属性](#72-orcparquet-相关属性)
<!-- /TOC -->
# 1. 管理表 vs 外部表 (Managed vs External Tables)

## 1.1 概念对比

| 维度 | 管理表 (MANAGED TABLE) | 外部表 (EXTERNAL TABLE) |
|------|----------------------|------------------------|
| 数据生命周期 | 随表删除而删除 | 删除表时数据保留 |
| DROP TABLE 行为 | 删除元数据 + HDFS 数据 | 仅删除元数据 |
| TRUNCATE TABLE | 支持 | 不支持 |
| 数据位置 | Hive 仓库目录（默认） | 用户指定路径（LOCATION） |
| 数据管理权 | Hive 完全管理 | 用户自行管理 |
| 适用场景 | ETL 中间表、临时表 | 原始日志、多引擎共享数据 |

## 1.2 创建语法对比

```sql
-- 管理表（默认）
CREATE TABLE managed_orders (
    order_id    BIGINT,
    user_id     BIGINT,
    amount      DECIMAL(10,2),
    order_date  DATE
)
STORED AS ORC;

-- 等价于显式声明
CREATE MANAGED TABLE managed_orders (
    order_id    BIGINT,
    user_id     BIGINT,
    amount      DECIMAL(10,2),
    order_date  DATE
)
STORED AS ORC;

-- 外部表
CREATE EXTERNAL TABLE external_logs (
    log_time    TIMESTAMP,
    user_id     BIGINT,
    event_type  STRING,
    event_data  STRING
)
ROW FORMAT DELIMITED
    FIELDS TERMINATED BY '\t'
STORED AS TEXTFILE
LOCATION '/data/logs/raw_events';
```

## 1.3 适用场景分析

```sql
-- 场景1: 原始日志数据 — 推荐外部表
-- 原因: 数据由 Flume/Logstash 等写入，不应被 Hive DROP 误删
CREATE EXTERNAL TABLE raw_web_logs (
    log_line STRING
)
STORED AS TEXTFILE
LOCATION '/ingest/web_logs/dt=2026-05-27';

-- 场景2: ETL 中间结果 — 推荐管理表
-- 原因: 中间表数据与表生命周期绑定，表删了数据自动清理
CREATE TABLE dwd_user_daily_summary (
    user_id      BIGINT,
    stat_date    DATE,
    pv           BIGINT,
    order_cnt    BIGINT,
    total_amount DECIMAL(18,2)
)
PARTITIONED BY (dt STRING)
STORED AS ORC;

-- 场景3: 多引擎共享数据 — 推荐外部表
-- 原因: Spark/Presto/Trino 也可直接读取同一份 HDFS 数据
CREATE EXTERNAL TABLE shared_user_profile (
    user_id    BIGINT,
    nick_name  STRING,
    city       STRING,
    gender     STRING
)
STORED AS PARQUET
LOCATION '/data/warehouse/shared/user_profile';
```

## 1.4 管理表与外部表互转

```sql
-- 查看表类型
DESCRIBE FORMATTED managed_orders;
-- 结果中 Table Type 字段:
-- MANAGED_TABLE  或  EXTERNAL_TABLE

-- 管理表 → 外部表
ALTER TABLE managed_orders SET TBLPROPERTIES('EXTERNAL'='TRUE');
-- DESCRIBE FORMATTED managed_orders;
-- Table Type: EXTERNAL_TABLE

-- 外部表 → 管理表
ALTER TABLE external_logs SET TBLPROPERTIES('EXTERNAL'='FALSE');
-- DESCRIBE FORMATTED external_logs;
-- Table Type: MANAGED_TABLE
```

# 2. 分区表 (Partitioned Tables)

## 2.1 分区概念和作用

```sql
说明：
    分区是将表的数据按照某个列（通常是日期、地区等）划分到不同的子目录中。
    查询时只扫描相关分区的数据，避免全表扫描，大幅提升查询效率。

作用：
    -- 物理层面: 数据按分区值存储在不同的 HDFS 子目录中
    -- 查询层面: WHERE 条件命中分区列时，自动跳过无关分区（分区裁剪）
    -- 管理层面: 可按分区粒度进行数据加载、删除、归档
```

## 2.2 单级分区

```sql
-- 创建单级分区表
CREATE TABLE orders_partitioned (
    order_id    BIGINT,
    user_id     BIGINT,
    amount      DECIMAL(10,2)
)
PARTITIONED BY (dt STRING COMMENT '日期分区，格式 YYYY-MM-DD')
STORED AS ORC;

-- HDFS 目录结构:
-- /warehouse/orders_partitioned/dt=2026-01-01/
-- /warehouse/orders_partitioned/dt=2026-01-02/
-- /warehouse/orders_partitioned/dt=2026-01-03/

-- 加载数据到指定分区
INSERT OVERWRITE TABLE orders_partitioned
PARTITION (dt = '2026-01-01')
SELECT order_id, user_id, amount
FROM source_orders
WHERE order_date = '2026-01-01';

-- 查看分区列表
SHOW PARTITIONS orders_partitioned;
-- 结果:
-- dt=2026-01-01
-- dt=2026-01-02
-- dt=2026-01-03

-- 新增分区
ALTER TABLE orders_partitioned ADD PARTITION (dt = '2026-01-04');

-- 删除分区
ALTER TABLE orders_partitioned DROP IF EXISTS PARTITION (dt = '2026-01-01');

-- 修复分区（当 HDFS 上已有目录但元数据缺失时）
MSCK REPAIR TABLE orders_partitioned;
```

## 2.3 多级分区

```sql
-- 创建多级分区表（日期 + 小时）
CREATE TABLE click_log (
    user_id      BIGINT,
    page_url     STRING,
    stay_seconds INT
)
PARTITIONED BY (dt STRING, hour STRING)
STORED AS ORC;

-- HDFS 目录结构:
-- /warehouse/click_log/dt=2026-05-27/hour=08/
-- /warehouse/click_log/dt=2026-05-27/hour=09/
-- /warehouse/click_log/dt=2026-05-27/hour=10/

-- 加载数据到二级分区
INSERT OVERWRITE TABLE click_log
PARTITION (dt = '2026-05-27', hour = '08')
SELECT user_id, page_url, stay_seconds
FROM raw_clicks
WHERE click_time >= '2026-05-27 08:00:00'
  AND click_time <  '2026-05-27 09:00:00';

-- 查询时指定二级分区
SELECT page_url, COUNT(*) AS pv
FROM click_log
WHERE dt = '2026-05-27'
  AND hour BETWEEN '08' AND '12'
GROUP BY page_url
ORDER BY pv DESC
LIMIT 10;
```

## 2.4 静态分区与动态分区

```sql
语法：
    静态分区: 在 INSERT 语句中显式指定分区值
    动态分区: 分区值从查询结果中的最后一列（或多列）自动推断

-- 静态分区示例（手动指定每个分区值）
INSERT OVERWRITE TABLE orders_partitioned
PARTITION (dt = '2026-01-01')
SELECT order_id, user_id, amount FROM source_orders
WHERE order_date = '2026-01-01';

INSERT OVERWRITE TABLE orders_partitioned
PARTITION (dt = '2026-01-02')
SELECT order_id, user_id, amount FROM source_orders
WHERE order_date = '2026-01-02';

-- 动态分区示例（自动根据查询结果的最后列分配分区）
-- 开启动态分区
SET hive.exec.dynamic.partition = true;
SET hive.exec.dynamic.partition.mode = nonstrict;
SET hive.exec.max.dynamic.partitions = 5000;
SET hive.exec.max.dynamic.partitions.pernode = 2000;

INSERT OVERWRITE TABLE orders_partitioned
PARTITION (dt)
SELECT order_id, user_id, amount, order_date AS dt
FROM source_orders;

-- 混合模式：一级静态 + 二级动态
INSERT OVERWRITE TABLE click_log
PARTITION (dt = '2026-05-27', hour)
SELECT user_id, page_url, stay_seconds, HOUR(click_time) AS hour
FROM raw_clicks
WHERE click_time >= '2026-05-27 00:00:00'
  AND click_time <  '2026-05-28 00:00:00';
```

## 2.5 分区裁剪

```sql
说明：
    分区裁剪（Partition Pruning）是 Hive 最重要的查询优化之一。
    当 WHERE 条件中包含分区列时，Hive 会自动跳过不匹配的分区目录，
    只读取满足条件的分区数据。

-- 分区裁剪生效示例
-- 假设表有 365 个日分区，此查询只扫描 1 个分区
SELECT COUNT(*) AS order_cnt, SUM(amount) AS total
FROM orders_partitioned
WHERE dt = '2026-01-15';
-- 仅扫描 /warehouse/orders_partitioned/dt=2026-01-15/

-- 范围裁剪
SELECT COUNT(*)
FROM orders_partitioned
WHERE dt >= '2026-01-01' AND dt <= '2026-01-07';
-- 扫描 7 个分区

-- 注意：对分区列使用函数会导致裁剪失效
SELECT COUNT(*)
FROM orders_partitioned
WHERE YEAR(dt) = 2026;
-- 全表扫描！应改为:
SELECT COUNT(*)
FROM orders_partitioned
WHERE dt >= '2026-01-01' AND dt <= '2026-12-31';
```

## 2.6 分区设计最佳实践

```sql
说明：
    1. 分区粒度适中: 避免过粗（单分区数据过大）和过细（大量小文件）
    2. 日分区最常用: 适合大部分业务场景
    3. 避免过多分区: Hive 元数据存储分区信息，数万分区会导致元数据查询缓慢
    4. 分区列不参与查询列: 分区列不在 SELECT 后的列中，仅做过滤条件

-- 反例: 按 user_id 分区会导致百万级分区，元数据查询极慢
CREATE TABLE bad_design (
    order_id   BIGINT,
    amount     DECIMAL(10,2)
)
PARTITIONED BY (user_id BIGINT);  -- 不推荐!

-- 正例: 按日期分区，按 user_id 用 ORC 排序优化查询
CREATE TABLE good_design (
    order_id   BIGINT,
    user_id    BIGINT,
    amount     DECIMAL(10,2)
)
PARTITIONED BY (dt STRING)
STORED AS ORC
TBLPROPERTIES ('orc.bloom.filter.columns'='user_id');

-- 分区数过多时的处理方案:
SET hive.metastore.limit.partition.request = 1000;
-- 对于历史分区，可以按年/月合并成更大的分区粒度
-- 或者将冷数据导出到低成本存储
```

# 3. 分桶表 (Bucketed / Clustered Tables)

## 3.1 分桶概念和作用

```sql
说明：
    分桶是将表数据按某列的哈希值分成固定数量的桶（文件）。
    每个桶是一个独立的 HDFS 文件，相同哈希值的行落在同一个桶中。
    
作用：
    1. 加速 JOIN: 两个表按相同的列分桶，JOIN 时只需匹配对应桶（SMB Join）
    2. 加速抽样: TABLESAMPLE 直接按桶抽样，无需全表扫描
    3. 数据均匀分布: 避免数据倾斜
```

## 3.2 CLUSTERED BY 语法

```sql
-- 创建分桶表
CREATE TABLE user_bucketed (
    user_id    BIGINT,
    user_name  STRING,
    age        INT,
    city       STRING
)
CLUSTERED BY (user_id) INTO 16 BUCKETS
STORED AS ORC;

-- HDFS 目录结构（每个桶一个文件）:
-- /warehouse/user_bucketed/000000_0
-- /warehouse/user_bucketed/000001_0
-- ...
-- /warehouse/user_bucketed/000015_0

-- 插入数据时必须开启动态分桶
SET hive.enforce.bucketing = true;
-- 或 (Hive 3.x+):
SET hive.optimize.bucketmapjoin = true;

INSERT INTO user_bucketed
SELECT user_id, user_name, age, city
FROM source_users;

-- 验证分桶情况
SELECT user_id, user_name
FROM user_bucketed
TABLESAMPLE(BUCKET 1 OUT OF 16 ON user_id)
LIMIT 10;
```

## 3.3 分桶与 SORTED BY

```sql
-- 分桶 + 桶内排序
CREATE TABLE orders_bucketed_sorted (
    order_id   BIGINT,
    user_id    BIGINT,
    amount     DECIMAL(10,2),
    order_date DATE
)
CLUSTERED BY (user_id) SORTED BY (order_date) INTO 32 BUCKETS
STORED AS ORC;

说明：
    SORTED BY 在每个桶内按指定列排序存储。
    配合 ORC 的谓词下推，范围查询时可以跳过不匹配的 stripe。

-- 加载数据
SET hive.enforce.bucketing = true;

INSERT INTO orders_bucketed_sorted
SELECT order_id, user_id, amount, order_date
FROM source_orders;

-- 查询优化: 按 user_id 范围查询时，ORC 可利用排序信息
SELECT SUM(amount) AS total
FROM orders_bucketed_sorted
WHERE user_id = 12345
  AND order_date >= '2026-01-01';
```

## 3.4 分桶与 JOIN 优化 (SMB Join)

```sql
说明：
    SMB (Sort Merge Bucket) Join:
    当两个表按相同的列、相同的桶数分桶并排序时，
    Hive 可以进行 SMB Join，每个 Mapper 只处理对应编号的桶，
    避免全表 Shuffle。

-- 创建两个分桶表，桶数和分桶列完全一致
CREATE TABLE orders_bucket (
    order_id   BIGINT,
    user_id    BIGINT,
    amount     DECIMAL(10,2)
)
CLUSTERED BY (user_id) SORTED BY (user_id) INTO 32 BUCKETS
STORED AS ORC;

CREATE TABLE users_bucket (
    user_id    BIGINT,
    user_name  STRING,
    city       STRING
)
CLUSTERED BY (user_id) SORTED BY (user_id) INTO 32 BUCKETS
STORED AS ORC;

-- 开启 SMB Join 参数
SET hive.optimize.bucketmapjoin = true;
SET hive.optimize.bucketmapjoin.sortedmerge = true;
SET hive.auto.convert.sortmerge.join = true;
SET hive.auto.convert.sortmerge.join.noconditionaltask = true;

-- SMB Join 查询
SELECT o.order_id, o.amount, u.user_name, u.city
FROM orders_bucket o
JOIN users_bucket u ON o.user_id = u.user_id
WHERE o.amount > 100;
```

## 3.5 分桶抽样 TABLESAMPLE

```sql
语法：
    SELECT ... FROM table_name TABLESAMPLE(BUCKET x OUT OF y [ON col_name])
    
说明：
    从 y 个桶中抽取第 x 个桶的数据。如果指定了 ON col_name，
    则按该列哈希分桶；否则使用建表时的 CLUSTERED BY 列。

-- 从 32 个桶中抽取第 1 个桶（约 1/32 的数据）
SELECT * FROM orders_bucketed_sorted
TABLESAMPLE(BUCKET 1 OUT OF 32 ON user_id);

-- 抽取连续 4 个桶（约 1/8 的数据）
SELECT * FROM orders_bucketed_sorted
TABLESAMPLE(BUCKET 1 OUT OF 8 ON user_id);
-- 相当于抽取桶 1, 9, 17, 25

-- 按 row_count 抽样（基于行数，非桶抽样）
SELECT * FROM orders_bucketed_sorted
TABLESAMPLE(100 ROWS);

-- 按百分比抽样（Block Sampling，非精确）
SELECT * FROM orders_bucketed_sorted
TABLESAMPLE(10 PERCENT);
```

## 3.6 分桶设计最佳实践

```sql
说明：
    1. 桶数选择: 通常设为 Mapper 数量的倍数，如 16、32、64、128
    2. 桶数列选择: 高频 JOIN 列、GROUP BY 列、分布均匀的列
    3. 分桶 + 排序: 查询有范围过滤时，SORTED BY 可提升 ORC 谓词下推效果
    4. 避免过度分桶: 桶数过多会产生大量小文件，增加 NameNode 压力
    5. 与分区结合: 先分区再分桶，每个分区内独立分桶
       PARTITIONED BY (dt STRING) CLUSTERED BY (user_id) INTO 64 BUCKETS
```

# 4. 文件格式 (File Formats / Storage Formats)

## 4.1 STORED AS 子句

```sql
语法：
    CREATE TABLE table_name (...) STORED AS file_format;

-- 等价写法（使用 STORED AS）
CREATE TABLE t1 (...) STORED AS ORC;
CREATE TABLE t2 (...) STORED AS PARQUET;
CREATE TABLE t3 (...) STORED AS TEXTFILE;

-- 等价写法（使用 INPUTFORMAT / OUTPUTFORMAT / SERDE）
CREATE TABLE t1 (...)
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.orc.OrcInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.orc.OrcOutputFormat';
```

## 4.2 TEXTFILE

```sql
说明：
    Hive 默认存储格式，行式存储。每行一条记录，数据以纯文本形式保存。
    优点是可读性好，任何工具都能直接查看；缺点是压缩率低、查询性能差。

-- 创建 TEXTFILE 表
CREATE TABLE text_users (
    user_id    BIGINT,
    user_name  STRING,
    age        INT,
    tags       ARRAY<STRING>,
    props      MAP<STRING,STRING>
)
ROW FORMAT DELIMITED
    FIELDS TERMINATED BY ','
    COLLECTION ITEMS TERMINATED BY '|'
    MAP KEYS TERMINATED BY ':'
STORED AS TEXTFILE;

-- HDFS 文件内容示例（可直接 cat 查看）:
-- 1001,张三,25,篮球|音乐|旅行,city:北京|level:vip
-- 1002,李四,30,足球|阅读,city:上海|level:normal
```

## 4.3 SEQUENCEFILE

```sql
说明：
    二进制 Key-Value 格式的行式存储，主要由 MapReduce 框架生成。
    支持压缩（Record/Block 两种压缩模式），但查询性能不如列式格式。
    在现代 Hive 实践中已较少使用，逐渐被 ORC/Parquet 替代。

-- 创建 SEQUENCEFILE 表
CREATE TABLE seq_data (
    key     STRING,
    value   STRING
)
STORED AS SEQUENCEFILE;

-- 适用场景: MapReduce 中间结果、与其他 Hadoop 组件交换数据
```

## 4.4 ORC

```sql
说明：
    ORC (Optimized Row Columnar) 是 Hive 最推荐的列式存储格式。
    特点:
    - 列式存储，只读取查询涉及的列，减少 I/O
    - 轻量级索引: 每 10000 行一个 stripe，含 min/max 统计
    - 内置 Bloom Filter 加速等值查询
    - 高压缩率，支持 Snappy/Zlib/LZO/ZSTD 压缩
    - 谓词下推: 利用 stripe/row group 级别的统计信息过滤数据

-- 创建 ORC 表（推荐配置）
CREATE TABLE orc_orders (
    order_id    BIGINT,
    user_id     BIGINT,
    amount      DECIMAL(10,2),
    order_date  DATE,
    status      STRING
)
PARTITIONED BY (dt STRING)
STORED AS ORC
TBLPROPERTIES (
    'orc.compress'='SNAPPY',
    'orc.create.index'='true',
    'orc.bloom.filter.columns'='user_id,order_id',
    'orc.stripe.size'='268435456',
    'orc.row.index.stride'='10000'
);

-- ORC 文件结构:
-- [File Footer (元数据)]
--   [Stripe #1]
--     [Index Data]  -- min/max/bloom filter 统计
--     [Row Data]    -- 列式数据块 (默认 10000 行)
--     [Stripe Footer]
--   [Stripe #2]
--   ...

-- 查看 ORC 文件的元数据
-- hive --orcfiledump /path/to/orc/file
```

## 4.5 PARQUET

```sql
说明：
    Parquet 是另一种主流列式存储格式，由 Cloudera 和 Twitter 联合开发。
    与 ORC 相比:
    - 跨平台兼容性更好: Spark、Presto、Trino、Flink 都原生支持
    - 嵌套列（struct/array/map）支持优秀，使用 Dremel 编码
    - 压缩率和查询性能与 ORC 接近
    - 适合多引擎数据湖场景

-- 创建 Parquet 表
CREATE TABLE parquet_events (
    event_id    BIGINT,
    user_id     BIGINT,
    event_type  STRING,
    event_data  STRING,
    props       MAP<STRING,STRING>
)
PARTITIONED BY (dt STRING)
STORED AS PARQUET
TBLPROPERTIES (
    'parquet.compression'='SNAPPY',
    'parquet.block.size'='134217728',
    'parquet.page.size'='1048576'
);

-- 将 ORC 表转换为 Parquet（跨格式 CTAS）
CREATE TABLE parquet_orders
STORED AS PARQUET
AS SELECT * FROM orc_orders;

-- 读取已有 Parquet 文件创建外部表
CREATE EXTERNAL TABLE ext_parquet_data (
    col1 STRING,
    col2 BIGINT,
    col3 DOUBLE
)
STORED AS PARQUET
LOCATION '/data/external/parquet_files';
```

## 4.6 AVRO

```sql
说明：
    Avro 是行式存储格式，最大特点是支持 Schema 演进（Schema Evolution）。
    数据文件中嵌入 Schema，读写时可使用不同的 Schema 版本（需兼容）。
    适合需要频繁变更列结构的数据管道场景。

-- 创建 Avro 表
CREATE TABLE avro_users (
    user_id    BIGINT,
    user_name  STRING,
    email      STRING
)
STORED AS AVRO
TBLPROPERTIES ('avro.schema.literal'='{
  "type": "record",
  "name": "User",
  "fields": [
    {"name": "user_id",   "type": "long"},
    {"name": "user_name", "type": "string"},
    {"name": "email",     "type": "string"}
  ]
}');

-- 或者从 HDFS 上的 Avro Schema 文件加载
CREATE TABLE avro_users_v2 (
    user_id    BIGINT,
    user_name  STRING,
    email      STRING,
    phone      STRING
)
STORED AS AVRO
TBLPROPERTIES ('avro.schema.url'='hdfs:///schemas/user.avsc');

-- Avro 适合: Kafka → Hive 数据管道、需要列变更的历史数据
```

## 4.7 RCFILE

```sql
说明：
    RCFile (Record Columnar File) 是 Hive 早期的列式存储格式。
    已被 ORC 完全取代，ORC 在压缩率、查询性能、索引等方面全面优于 RCFile。
    仅需了解其存在，不推荐在新项目中使用。

-- 创建 RCFILE 表（仅作了解）
CREATE TABLE rcfile_demo (
    col1 STRING,
    col2 INT
)
STORED AS RCFILE;
```

## 4.8 各格式对比与推荐

| 格式 | 存储方式 | 压缩率 | 查询性能 | 写入性能 | Schema演进 | 跨平台 |
|------|---------|--------|---------|---------|-----------|--------|
| TEXTFILE | 行式 | 无/低 | 低 | 高 | 否 | 极好 |
| SEQUENCEFILE | 行式(KV) | 中 | 中 | 中 | 否 | 好 |
| RCFILE | 列式(早期) | 中 | 中 | 中 | 否 | 差 |
| **ORC** | 列式 | **高** | **高** | 中 | 有限 | 中 |
| **Parquet** | 列式 | **高** | **高** | 中 | 有限 | **极好** |
| Avro | 行式 | 中 | 低 | 高 | **极好** | 好 |

```sql
说明：
推荐策略:
    -- Hive 内部数仓表: 首选 ORC
    -- 多引擎数据湖: 首选 Parquet
    -- 原始数据接入层: TEXTFILE 或 Avro（如需 Schema 演进）
    -- ETL 中间临时表: ORC + 高压缩率 (Zlib)
    -- 查询密集型表: ORC + Snappy 压缩 + Bloom Filter
```

# 5. 压缩 (Compression)

## 5.1 压缩配置

```sql
-- 开启 MapReduce 中间结果压缩（减少 Shuffle 数据量）
SET hive.exec.compress.intermediate = true;
SET mapred.map.output.compression.codec = org.apache.hadoop.io.compress.SnappyCodec;

-- 开启最终输出压缩
SET hive.exec.compress.output = true;

-- ORC 输出压缩
SET hive.exec.orc.compression.strategy = SPEED;
-- 可选: SPEED（速度快）| COMPRESSION（压缩率高）

-- Parquet 输出压缩
SET parquet.compression = SNAPPY;
-- 可选: SNAPPY | GZIP | LZO | ZSTD | NONE

-- 检查当前压缩设置
SET hive.exec.compress.intermediate;
SET hive.exec.compress.output;
SET mapred.map.output.compression.codec;
```

## 5.2 常用压缩编码

```sql
语法：
    压缩编码通过 TBLPROPERTIES 在建表时指定，或通过 SET 命令全局配置。

-- Snappy (推荐)
-- 优点: 压缩/解压速度极快，CPU 开销低
-- 缺点: 压缩率中等
CREATE TABLE orc_snappy (...)
STORED AS ORC
TBLPROPERTIES ('orc.compress'='SNAPPY');

-- Zlib (Gzip)
-- 优点: 压缩率最高，节省存储空间
-- 缺点: 压缩/解压速度慢，CPU 开销大
CREATE TABLE orc_gzip (...)
STORED AS ORC
TBLPROPERTIES ('orc.compress'='ZLIB');

-- LZO
-- 优点: 压缩/解压速度快，支持 Splittable
-- 缺点: 压缩率低于 Zlib，需要额外安装 native 库
-- 注意: LZO 文件索引需要手动建立
-- hadoop jar /path/to/hadoop-lzo.jar com.hadoop.compression.lzo.LzoIndexer /path/to/files

-- ZSTD (Facebook 开源)
-- 优点: 压缩率接近 Zlib，解压速度接近 Snappy
-- 缺点: Hive 版本要求较高（3.x+）
CREATE TABLE orc_zstd (...)
STORED AS ORC
TBLPROPERTIES ('orc.compress'='ZSTD');

-- LZ4
-- 优点: 解压速度极快
-- 缺点: 压缩率最低
CREATE TABLE orc_lz4 (...)
STORED AS ORC
TBLPROPERTIES ('orc.compress'='LZ4');
```

## 5.3 中间结果压缩 vs 最终输出压缩

```sql
说明：
    -- 中间结果压缩: Map 端输出 → Reduce 端输入的 Shuffle 阶段
    -- 最终输出压缩: 写入 HDFS 最终文件时的压缩

-- 中间结果压缩（关键优化，强烈建议开启）
SET hive.exec.compress.intermediate = true;
SET mapred.map.output.compression.codec = org.apache.hadoop.io.compress.SnappyCodec;
-- 此配置对性能影响最大：减少 Shuffle 网络传输，且 Snappy 压缩/解压 CPU 开销极低

-- 最终输出压缩
SET hive.exec.compress.output = true;
SET mapred.output.compression.codec = org.apache.hadoop.io.compress.SnappyCodec;
-- 适用于 TEXTFILE/SEQUENCEFILE；ORC/Parquet 由 TBLPROPERTIES 控制

-- 选择策略:
--   中间结果: Snappy (追求速度)
--   热数据:   Snappy 或 ZSTD (平衡速度与空间)
--   冷数据:   Zlib 或 ZSTD (追求压缩率)
--   归档数据: Zlib (最高压缩率)
```

## 5.4 各格式默认压缩

| 格式 | 默认压缩 | 可配置属性 |
|------|---------|-----------|
| TEXTFILE | 无 | `mapred.output.compression.codec` |
| SEQUENCEFILE | 无 | `mapred.output.compression.codec` |
| ORC | ZLIB | `orc.compress` (`TBLPROPERTIES`) |
| Parquet | 无/UNCOMPRESSED | `parquet.compression` (`TBLPROPERTIES`) |
| Avro | 无 | `avro.output.codec` (`TBLPROPERTIES`) |

# 6. SerDe (序列化/反序列化)

## 6.1 SerDe 概念

```sql
说明：
    SerDe = Serializer（序列化） + Deserializer（反序列化）
    
    -- 读数据 (Deserialize): HDFS 文件中的字节 → Hive 表中的行对象
    -- 写数据 (Serialize): Hive 表中的行对象 → HDFS 文件中的字节
    
    Hive 通过 SerDe 理解各种文件格式的内容，是 Hive 可扩展性的核心机制。
```

## 6.2 ROW FORMAT SERDE 语法

```sql
语法：
    -- 方式一: 使用内置 SerDe + 自定义分隔符
    CREATE TABLE t1 (...)
    ROW FORMAT DELIMITED
        FIELDS TERMINATED BY '\t'
        COLLECTION ITEMS TERMINATED BY ','
        MAP KEYS TERMINATED BY ':'
        LINES TERMINATED BY '\n'
    STORED AS TEXTFILE;

    -- 方式二: 显式指定 SerDe 类名
    CREATE TABLE t2 (...)
    ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe'
    WITH SERDEPROPERTIES (
        'field.delim' = '\t',
        'collection.delim' = ',',
        'mapkey.delim' = ':'
    )
    STORED AS TEXTFILE;
```

## 6.3 常用 SerDe

```sql
-- 1. LazySimpleSerDe（默认 SerDe）
-- 适用于 TEXTFILE 格式，支持自定义分隔符
CREATE TABLE lazy_demo (
    col1 STRING,
    col2 INT,
    col3 DOUBLE
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe'
WITH SERDEPROPERTIES ('field.delim' = '|')
STORED AS TEXTFILE;

-- 2. JsonSerDe
-- 解析 JSON 格式数据（每行一个 JSON 对象）
CREATE TABLE json_logs (
    user_id    BIGINT,
    event_type STRING,
    timestamp  STRING,
    event_data MAP<STRING,STRING>
)
ROW FORMAT SERDE 'org.apache.hive.hcatalog.data.JsonSerDe'
STORED AS TEXTFILE;
-- 数据示例: {"user_id":1001,"event_type":"click","timestamp":"2026-05-27 10:00:00","event_data":{"page":"home","button":"login"}}

-- 3. RegexSerDe
-- 按正则表达式解析非结构化日志
CREATE TABLE apache_logs (
    host       STRING,
    identity   STRING,
    user_name  STRING,
    time       STRING,
    request    STRING,
    status     INT,
    size       INT
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.RegexSerDe'
WITH SERDEPROPERTIES (
    'input.regex' = '^([^ ]*) ([^ ]*) ([^ ]*) \\[([^\\]]*)\\] "([^"]*)" (\\d+) (\\d+)$'
)
STORED AS TEXTFILE;
-- 数据示例: 192.168.1.1 - - [27/May/2026:10:00:00 +0800] "GET /index.html HTTP/1.1" 200 1024

-- 4. CSVSerDe
-- 解析 CSV 文件，正确处理引号内逗号
CREATE TABLE csv_data (
    id      INT,
    name    STRING,
    city    STRING,
    score   DOUBLE
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.OpenCSVSerde'
WITH SERDEPROPERTIES (
    'separatorChar' = ',',
    'quoteChar'     = '"',
    'escapeChar'    = '\\'
)
STORED AS TEXTFILE
TBLPROPERTIES ('skip.header.line.count' = '1');
-- 数据示例: 1,"Smith, John","New York",95.5

-- 5. ORC SerDe 和 Parquet SerDe（通常无需显式指定）
-- STORED AS ORC 自动使用 OrcSerDe
-- STORED AS PARQUET 自动使用 ParquetHiveSerDe
CREATE TABLE orc_auto (...) STORED AS ORC;
-- 等价于:
CREATE TABLE orc_auto (...)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.orc.OrcSerde'
STORED AS INPUTFORMAT 'org.apache.hadoop.hive.ql.io.orc.OrcInputFormat'
OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.orc.OrcOutputFormat';
```

## 6.4 自定义分隔符

```sql
-- ROW FORMAT DELIMITED 方式
CREATE TABLE delim_demo (
    user_id    BIGINT,
    user_name  STRING,
    age        INT
)
ROW FORMAT DELIMITED
    FIELDS TERMINATED BY '\t'         -- 列分隔符
    LINES TERMINATED BY '\n'          -- 行分隔符（默认）
STORED AS TEXTFILE;

-- 使用多字符分隔符（需通过 SerDe 属性）
CREATE TABLE multi_char_delim (
    col1 STRING,
    col2 STRING,
    col3 STRING
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe'
WITH SERDEPROPERTIES ('field.delim' = '||')
STORED AS TEXTFILE;
-- 数据示例: value1||value2||value3

-- Hive 默认分隔符（Ctrl+A, Ctrl+B, Ctrl+C）
-- FIELDS TERMINATED BY '\001' (^A)
-- COLLECTION ITEMS TERMINATED BY '\002' (^B)
-- MAP KEYS TERMINATED BY '\003' (^C)

-- 查看表的分隔符配置
DESCRIBE FORMATTED delim_demo;
```

## 6.5 COLLECTION ITEMS 与 MAP KEYS

```sql
-- 复杂类型的分隔符层次结构
CREATE TABLE complex_delim (
    user_id    BIGINT,
    user_name  STRING,
    tags       ARRAY<STRING>,
    scores     MAP<STRING,INT>,
    address    STRUCT<city:STRING, street:STRING, zip:STRING>
)
ROW FORMAT DELIMITED
    FIELDS TERMINATED BY ','               -- 第一层级: 列之间
    COLLECTION ITEMS TERMINATED BY '|'     -- 第二层级: ARRAY 元素、STRUCT 字段之间
    MAP KEYS TERMINATED BY ':'             -- 第三层级: MAP 的 Key-Value 之间
STORED AS TEXTFILE;

-- 对应文本数据示例:
-- 1001,张三,篮球|音乐|旅行,math:95|english:88,北京|长安街|100000

-- 解析规则:
-- 第一层 (FIELDS):      1001 | 张三 | 篮球|音乐|旅行 | math:95|english:88 | 北京|长安街|100000
-- 第二层 (COLLECTION):   tags: 篮球, 音乐, 旅行
--                        address: 北京, 长安街, 100000
-- 第三层 (MAP KEYS):     math:95, english:88

-- 插入数据的两种方式
-- 方式1: 通过 CTAS 或 INSERT SELECT 从其他表导入
INSERT INTO complex_delim
SELECT 1001, '张三',
       ARRAY('篮球','音乐','旅行'),
       MAP('math', 95, 'english', 88),
       NAMED_STRUCT('city','北京','street','长安街','zip','100000');

-- 方式2: 直接上传符合分隔规则的文件到 HDFS LOAD DATA
-- LOAD DATA LOCAL INPATH '/tmp/complex_data.txt' INTO TABLE complex_delim;
```

# 7. 表属性 (Table Properties)

## 7.1 TBLPROPERTIES 常用配置

```sql
-- 跳过表头行
CREATE TABLE csv_with_header (
    col1 STRING,
    col2 STRING,
    col3 STRING
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.OpenCSVSerde'
STORED AS TEXTFILE
TBLPROPERTIES ('skip.header.line.count' = '1');
-- 数据示例:
-- col1,col2,col3          ← 这行被跳过
-- a,b,c
-- d,e,f

-- 设置表的备注信息
CREATE TABLE annotated_table (
    id   INT,
    name STRING
)
COMMENT '这是用户基础信息表，存储核心用户数据'
STORED AS ORC;

-- 设置表级别自定义属性（可被 Spark/Presto 等引擎读取）
ALTER TABLE annotated_table SET TBLPROPERTIES (
    'created_by' = 'data_team',
    'data_domain' = 'user',
    'retention_days' = '365',
    'privacy_level' = 'internal'
);

-- 查看表属性
SHOW TBLPROPERTIES annotated_table;
-- 结果:
-- COMMENT              这是用户基础信息表，存储核心用户数据
-- created_by           data_team
-- data_domain          user
-- retention_days       365
-- privacy_level        internal
-- ...

-- 设置表不可删除/不可修改（保护关键表）
ALTER TABLE critical_table SET TBLPROPERTIES ('auto.purge' = 'false');
ALTER TABLE critical_table SET TBLPROPERTIES ('EXTERNAL' = 'TRUE');

-- 自动清理回收站数据
ALTER TABLE temp_table SET TBLPROPERTIES ('auto.purge' = 'true');
-- DROP TABLE temp_table 后数据直接删除，不进回收站
```

## 7.2 ORC/Parquet 相关属性

```sql
-- ORC 表属性详解
CREATE TABLE orc_optimized (
    user_id    BIGINT,
    user_name  STRING,
    age        INT,
    city       STRING,
    tags       ARRAY<STRING>
)
PARTITIONED BY (dt STRING)
STORED AS ORC
TBLPROPERTIES (
    'orc.compress'                 = 'SNAPPY',
    'orc.compress.size'            = '262144',
    'orc.stripe.size'              = '268435456',
    'orc.row.index.stride'         = '10000',
    'orc.create.index'             = 'true',
    'orc.bloom.filter.columns'     = 'user_id,city',
    'orc.bloom.filter.fpp'         = '0.05',
    'orc.bloom.filter.write.version' = '1.0',
    'orc.write.format'             = '0.12'
);
-- 属性说明:
-- orc.compress:              压缩编码 (NONE/ZLIB/SNAPPY/LZO/ZSTD)
-- orc.compress.size:         压缩块大小
-- orc.stripe.size:           每个 Stripe 的大小（默认 256MB）
-- orc.row.index.stride:     索引间隔行数（默认 10000）
-- orc.bloom.filter.columns:  创建 Bloom Filter 的列名（逗号分隔）
-- orc.bloom.filter.fpp:      Bloom Filter 误判率（默认 0.05）

-- Parquet 表属性详解
CREATE TABLE parquet_optimized (
    event_id   BIGINT,
    user_id    BIGINT,
    event_type STRING,
    event_data STRING
)
PARTITIONED BY (dt STRING)
STORED AS PARQUET
TBLPROPERTIES (
    'parquet.compression'          = 'SNAPPY',
    'parquet.block.size'           = '134217728',
    'parquet.page.size'            = '1048576',
    'parquet.dictionary.page.size' = '1048576',
    'parquet.enable.dictionary'    = 'true',
    'parquet.writer.version'       = 'v2'
);
-- 属性说明:
-- parquet.compression:          压缩编码 (UNCOMPRESSED/SNAPPY/GZIP/LZO/ZSTD)
-- parquet.block.size:           Row Group 大小（默认 128MB）
-- parquet.page.size:            Page 大小（默认 1MB）
-- parquet.dictionary.page.size: 字典编码页大小
-- parquet.enable.dictionary:    是否启用字典编码

-- 查看 ORC/Parquet 文件压缩情况
DESCRIBE FORMATTED orc_optimized PARTITION (dt = '2026-05-27');
-- 关注以下字段:
-- InputFormat:      org.apache.hadoop.hive.ql.io.orc.OrcInputFormat
-- OutputFormat:     org.apache.hadoop.hive.ql.io.orc.OrcOutputFormat
-- SerDe Library:    org.apache.hadoop.hive.ql.io.orc.OrcSerde
-- parameters:       orc.compress=SNAPPY, orc.bloom.filter.columns=user_id,city, ...
```