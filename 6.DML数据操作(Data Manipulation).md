<!-- TOC  -->
- [6. 数据操作（DML: Data Manipulation Language）](#6-数据操作dml-data-manipulation-language)
    - [6.1 LOAD DATA（加载数据）](#61-load-data加载数据)
        - [6.1.1 LOAD DATA LOCAL INPATH（从本地加载）](#611-load-data-local-inpath从本地加载)
        - [6.1.2 LOAD DATA INPATH（从HDFS加载）](#612-load-data-inpath从hdfs加载)
        - [6.1.3 OVERWRITE vs INTO](#613-overwrite-vs-into)
        - [6.1.4 LOAD DATA 到分区表](#614-load-data-到分区表)
    - [6.2 INSERT（插入数据）](#62-insert插入数据)
        - [6.2.1 INSERT INTO（追加）](#621-insert-into追加)
        - [6.2.2 INSERT OVERWRITE（覆盖）](#622-insert-overwrite覆盖)
        - [6.2.3 INSERT INTO/OVERWRITE 分区表](#623-insert-intooverwrite-分区表)
        - [6.2.4 从查询插入: INSERT INTO ... SELECT ...](#624-从查询插入-insert-into--select-)
        - [6.2.5 多表插入（Multi-Insert）](#625-多表插入multi-insert)
        - [6.2.6 INSERT ... VALUES（直接插入值）](#626-insert--values直接插入值)
        - [6.2.7 INSERT ... DIRECTORY（导出到文件系统）](#627-insert--directory导出到文件系统)
    - [6.3 动态分区插入（Dynamic Partition Insert）](#63-动态分区插入dynamic-partition-insert)
        - [6.3.1 动态分区与静态分区](#631-动态分区与静态分区)
        - [6.3.2 启用动态分区](#632-启用动态分区)
        - [6.3.3 混合使用静态和动态分区](#633-混合使用静态和动态分区)
        - [6.3.4 相关配置参数](#634-相关配置参数)
    - [6.4 UPDATE 和 DELETE](#64-update-和-delete)
        - [6.4.1 UPDATE](#641-update)
        - [6.4.2 DELETE](#642-delete)
        - [6.4.3 MERGE](#643-merge)
    - [6.5 EXPORT / IMPORT](#65-export--import)
        - [6.5.1 EXPORT TABLE TO](#651-export-table-to)
        - [6.5.2 IMPORT TABLE FROM](#652-import-table-from)
    - [6.6 TRUNCATE](#66-truncate)
        - [6.6.1 TRUNCATE TABLE](#661-truncate-table)
        - [6.6.2 TRUNCATE 分区](#662-truncate-分区)
<!-- /TOC -->
# 6. 数据操作（DML: Data Manipulation Language）
## 6.1 LOAD DATA（加载数据）
### 6.1.1 LOAD DATA LOCAL INPATH（从本地加载）
语法：
    LOAD DATA LOCAL INPATH 'filepath' [OVERWRITE] INTO TABLE tablename [PARTITION (partcol1=val1, partcol2=val2 ...)]

说明：
    从本地文件系统加载文件到Hive表中。文件会被复制到Hive仓库目录下，源文件不受影响。

举例：
    -- 从本地 /tmp/data/ 目录加载 employee.txt 到 employee 表
    LOAD DATA LOCAL INPATH '/tmp/data/employee.txt'
    OVERWRITE INTO TABLE employee;

    -- 从本地加载到分区表
    LOAD DATA LOCAL INPATH '/tmp/data/employee_2026.txt'
    INTO TABLE employee PARTITION (year=2026, month=5);
```

### 6.1.2 LOAD DATA INPATH（从HDFS加载）
语法：
    LOAD DATA INPATH 'hdfs_path' [OVERWRITE] INTO TABLE tablename [PARTITION (partcol1=val1, ...)]

说明：
    从HDFS加载文件到Hive表中。与LOCAL不同，该操作会**移动**文件而非复制。加载完成后，HDFS上源路径下的文件将被移除。

举例：
    -- 从HDFS加载数据
    LOAD DATA INPATH '/user/hive/warehouse/staging/employee.txt'
    INTO TABLE employee;

    -- 从HDFS加载并覆盖已有数据
    LOAD DATA INPATH '/user/hive/warehouse/staging/employee_new.txt'
    OVERWRITE INTO TABLE employee;
```

### 6.1.3 OVERWRITE vs INTO
说明：
    - `INTO`：追加模式，新数据会追加到表中，原有数据保留。
    - `OVERWRITE`：覆盖模式，新数据会替换表中已有数据（或指定分区的数据）。

举例：
    -- 追加数据（原数据保留）
    LOAD DATA LOCAL INPATH '/tmp/data/log_20260526.txt'
    INTO TABLE logs;

    -- 覆盖数据（原数据被替换）
    LOAD DATA LOCAL INPATH '/tmp/data/log_20260527.txt'
    OVERWRITE INTO TABLE logs;
```

### 6.1.4 LOAD DATA 到分区表
说明：
    LOAD DATA支持将数据直接加载到指定分区。如果未指定分区，则加载到表默认位置。

举例：
    -- 创建分区表
    CREATE TABLE sales (
        id INT,
        amount DOUBLE,
        sale_date DATE
    )
    PARTITIONED BY (year INT, month INT);

    -- 加载数据到指定分区
    LOAD DATA LOCAL INPATH '/tmp/data/sales_202605.txt'
    INTO TABLE sales PARTITION (year=2026, month=5);

    -- 加载数据时覆盖指定分区
    LOAD DATA LOCAL INPATH '/tmp/data/sales_202605_v2.txt'
    OVERWRITE INTO TABLE sales PARTITION (year=2026, month=5);
```

## 6.2 INSERT（插入数据）
### 6.2.1 INSERT INTO（追加）
语法：
    INSERT INTO TABLE tablename [PARTITION (partcol1=val1, ...)] select_statement;

说明：
    向表中追加数据，原有数据保持不变。对于分区表，如果不指定分区列，Hive会自动按最后一列为动态分区。

举例：
    -- 向 employee 表追加数据
    INSERT INTO TABLE employee
    SELECT id, name, salary, dept FROM new_employees;

    -- 向分区表追加数据
    INSERT INTO TABLE employee PARTITION (year=2026, month=5)
    SELECT id, name, salary FROM temp_employees;
```

### 6.2.2 INSERT OVERWRITE（覆盖）
语法：
    INSERT OVERWRITE TABLE tablename [PARTITION (partcol1=val1, ...)] select_statement;

说明：
    覆盖表中已有数据。对于非分区表，会清空整个表再写入；对于分区表，只覆盖指定分区的数据。

举例：
    -- 覆盖整个表
    INSERT OVERWRITE TABLE employee
    SELECT id, name, salary, dept FROM latest_employees;

    -- 覆盖指定分区的数据
    INSERT OVERWRITE TABLE sales PARTITION (year=2026, month=5)
    SELECT id, amount, sale_date FROM sales_staging;
```

### 6.2.3 INSERT INTO/OVERWRITE 分区表
说明：
    对于分区表，INSERT时可以通过PARTITION子句指定静态分区值，也可以使用动态分区自动推断分区值。

举例：
    -- 静态分区：明确指定分区值
    INSERT INTO TABLE logs PARTITION (dt='2026-05-27', hour='10')
    SELECT user_id, action, timestamp FROM raw_logs
    WHERE dt='2026-05-27' AND hour='10';

    -- 动态分区：由查询结果决定分区值（详见 6.3 节）
    INSERT INTO TABLE logs PARTITION (dt, hour)
    SELECT user_id, action, timestamp, dt, hour FROM raw_logs;
```

### 6.2.4 从查询插入: INSERT INTO ... SELECT ...
语法：
    INSERT INTO TABLE tablename SELECT ... FROM source_table [WHERE ...];

说明：
    最常见的插入方式，将查询语句的结果集插入目标表。支持JOIN、聚合、子查询等复杂查询。

举例：
    -- 从复杂查询插入
    INSERT INTO TABLE high_value_customers
    SELECT
        c.id,
        c.name,
        SUM(o.amount) AS total_spent
    FROM customers c
    JOIN orders o ON c.id = o.customer_id
    WHERE o.order_date >= '2026-01-01'
    GROUP BY c.id, c.name
    HAVING SUM(o.amount) > 10000;
```

### 6.2.5 多表插入（Multi-Insert）
语法：
    FROM source_table
    INSERT INTO TABLE tablename1 [PARTITION (...)] SELECT_statement1
    INSERT INTO TABLE tablename2 [PARTITION (...)] SELECT_statement2
    [INSERT OVERWRITE TABLE tablename3 [PARTITION (...)] SELECT_statement3 ...];

说明：
    在一次扫描源表的过程中，将数据同时插入到多个目标表中，大幅提高效率。可以混合使用INTO和OVERWRITE。

举例：
    -- 一次扫描，多表输出
    FROM sales
    INSERT INTO TABLE sales_all
        SELECT * WHERE amount > 0
    INSERT INTO TABLE sales_high
        SELECT * WHERE amount > 5000
    INSERT OVERWRITE TABLE sales_summary PARTITION (year=2026)
        SELECT id, amount, sale_date WHERE year(sale_date) = 2026;

    -- 结合分区，一次写入多个分区
    FROM daily_logs
    INSERT INTO TABLE logs PARTITION (dt='2026-05-26')
        SELECT user_id, action WHERE dt='2026-05-26'
    INSERT INTO TABLE logs PARTITION (dt='2026-05-27')
        SELECT user_id, action WHERE dt='2026-05-27';
```

### 6.2.6 INSERT ... VALUES（直接插入值）
语法：
    INSERT INTO TABLE tablename [PARTITION (...)] VALUES (val1, val2, ...), (val1, val2, ...), ...;

说明：
    直接向表中插入字面量值，适用于少量数据的快速插入。Hive 0.14 版本开始支持。

举例：
    -- 插入单行
    INSERT INTO TABLE employee VALUES
    (101, '张三', 15000.00, '技术部');

    -- 插入多行
    INSERT INTO TABLE employee VALUES
    (102, '李四', 18000.00, '市场部'),
    (103, '王五', 20000.00, '技术部'),
    (104, '赵六', 16000.00, '财务部');

    -- 插入到分区表
    INSERT INTO TABLE employee PARTITION (year=2026, month=5) VALUES
    (105, '孙七', 12000.00, '人事部');
```

### 6.2.7 INSERT ... DIRECTORY（导出到文件系统）
语法：
    INSERT OVERWRITE [LOCAL] DIRECTORY 'path'
    [ROW FORMAT row_format] [STORED AS file_format]
    SELECT_statement;

说明：
    将查询结果直接导出到文件系统目录中。`LOCAL`表示导出到本地文件系统，否则导出到HDFS。支持指定行格式和存储格式。

举例：
    -- 导出到HDFS目录
    INSERT OVERWRITE DIRECTORY '/user/hive/export/employee_export'
    ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
    SELECT * FROM employee WHERE salary > 15000;

    -- 导出到本地文件系统
    INSERT OVERWRITE LOCAL DIRECTORY '/tmp/export/employee_csv'
    ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
    STORED AS TEXTFILE
    SELECT id, name, salary FROM employee;

    -- 多文件导出（一次查询导出到多个目录）
    FROM employee
    INSERT OVERWRITE DIRECTORY '/user/hive/export/tech'
        SELECT * WHERE dept = '技术部'
    INSERT OVERWRITE DIRECTORY '/user/hive/export/finance'
        SELECT * WHERE dept = '财务部';
```

## 6.3 动态分区插入（Dynamic Partition Insert）
### 6.3.1 动态分区与静态分区
说明：
    - **静态分区**：在INSERT时明确指定分区值，如 `PARTITION (year=2026)`。
    - **动态分区**：分区值由查询结果的最后几列自动推导，如 `PARTITION (year, month)`。
    - 动态分区适合分区值不确定、需要根据数据内容自动分区的场景。

举例：
    -- 静态分区：分区值硬编码
    INSERT INTO TABLE logs PARTITION (dt='2026-05-27')
    SELECT user_id, action, timestamp FROM raw_logs;

    -- 动态分区：分区值来自查询列
    INSERT INTO TABLE logs PARTITION (dt)
    SELECT user_id, action, timestamp, dt FROM raw_logs;
    -- dt列作为分区值，不会写入表中
```

### 6.3.2 启用动态分区
说明：
    使用动态分区前需要设置相关参数。核心参数为 `hive.exec.dynamic.partition`。

举例：
    -- 设置允许动态分区
    SET hive.exec.dynamic.partition = true;

    -- 设置动态分区模式为非严格模式（允许所有分区列都是动态的）
    SET hive.exec.dynamic.partition.mode = nonstrict;

    -- 创建目标分区表
    CREATE TABLE sales_dynamic (
        id INT,
        amount DOUBLE
    )
    PARTITIONED BY (year INT, month INT);

    -- 动态分区插入：查询结果的后两列自动作为分区值
    INSERT INTO TABLE sales_dynamic PARTITION (year, month)
    SELECT id, amount, year, month FROM sales_staging;
    -- 结果：数据按 year 和 month 列的值自动分布到不同分区
```

### 6.3.3 混合使用静态和动态分区
说明：
    可以在同一个INSERT语句中同时使用静态分区和动态分区。静态分区必须在动态分区之前声明。

举例：
    -- year使用静态分区，month使用动态分区
    INSERT INTO TABLE sales_dynamic PARTITION (year=2026, month)
    SELECT id, amount, month FROM sales_2026_staging;
    -- 所有数据写入 year=2026 下的各个 month 分区

    -- 多级分区混合使用
    INSERT INTO TABLE web_logs PARTITION (year=2026, month, day)
    SELECT user_id, url, month, day FROM staging_logs;
    -- year 固定为 2026，month 和 day 由数据动态决定
```

### 6.3.4 相关配置参数

| 参数名 | 默认值 | 说明 |
|---|---|---|
| `hive.exec.dynamic.partition` | `false` | 是否启用动态分区 |
| `hive.exec.dynamic.partition.mode` | `strict` | strict: 至少一个分区列为静态；nonstrict: 允许全动态分区 |
| `hive.exec.max.dynamic.partitions` | `1000` | 单个语句允许创建的最大动态分区数 |
| `hive.exec.max.dynamic.partitions.pernode` | `100` | 每个节点允许创建的最大动态分区数 |
| `hive.exec.max.created.files` | `100000` | 全局最多创建的文件数 |
| `hive.error.on.empty.partition` | `false` | 当动态分区插入产生空分区时是否抛出异常 |

举例：
    -- 调大允许的动态分区数
    SET hive.exec.max.dynamic.partitions = 5000;
    SET hive.exec.max.dynamic.partitions.pernode = 500;

    INSERT INTO TABLE logs PARTITION (dt, hour)
    SELECT * FROM staging_logs;
```

## 6.4 UPDATE 和 DELETE
### 6.4.1 UPDATE
语法：
    UPDATE tablename SET col1 = val1, col2 = val2, ... [WHERE condition];

说明：
    UPDATE和DELETE仅在支持ACID事务的表中可用（Hive 0.14+）。建表时需要指定 `TBLPROPERTIES ('transactional'='true')`，且表必须分桶且使用ORC格式存储。

举例：
    -- 创建ACID事务表
    CREATE TABLE employee_acid (
        id INT,
        name STRING,
        salary DOUBLE,
        dept STRING
    )
    CLUSTERED BY (id) INTO 5 BUCKETS
    STORED AS ORC
    TBLPROPERTIES ('transactional'='true');

    -- 插入初始数据
    INSERT INTO TABLE employee_acid VALUES
    (1, '张三', 10000.00, '技术部'),
    (2, '李四', 12000.00, '市场部'),
    (3, '王五', 15000.00, '技术部');

    -- 更新指定行的工资
    UPDATE employee_acid SET salary = 18000.00 WHERE id = 1;

    -- 更新多个列
    UPDATE employee_acid SET salary = 20000.00, dept = '财务部' WHERE name = '王五';

    -- 查询结果
    SELECT * FROM employee_acid;
    -- +-----+-------+---------+-----------+
    -- | id  | name  | salary  | dept      |
    -- +-----+-------+---------+-----------+
    -- | 1   | 张三  | 18000.0 | 技术部    |
    -- | 2   | 李四  | 12000.0 | 市场部    |
    -- | 3   | 王五  | 20000.0 | 财务部    |
    -- +-----+-------+---------+-----------+
```

### 6.4.2 DELETE
语法：
    DELETE FROM tablename [WHERE condition];

说明：
    从ACID事务表中删除满足条件的行。如果不加WHERE条件，会删除表中所有行（但比TRUNCATE慢）。

举例：
    -- 删除指定行
    DELETE FROM employee_acid WHERE id = 2;

    -- 按条件批量删除
    DELETE FROM employee_acid WHERE salary < 15000 AND dept = '技术部';

    -- 查询结果
    SELECT * FROM employee_acid;
    -- +-----+-------+---------+-----------+
    -- | id  | name  | salary  | dept      |
    -- +-----+-------+---------+-----------+
    -- | 3   | 王五  | 20000.0 | 财务部    |
    -- +-----+-------+---------+-----------+
```

### 6.4.3 MERGE
语法：
    MERGE INTO target_table AS T
    USING source_table AS S
    ON T.key = S.key
    WHEN MATCHED [AND condition] THEN UPDATE SET ...
    WHEN MATCHED [AND condition] THEN DELETE
    WHEN NOT MATCHED [AND condition] THEN INSERT VALUES ...;

说明：
    MERGE（Hive 2.2+）根据源表与目标表的匹配情况，执行UPDATE、DELETE或INSERT操作，一条语句完成数据同步。目标表必须是ACID事务表。

举例：
    -- 创建源表（增量数据）
    CREATE TABLE employee_delta (
        id INT,
        name STRING,
        salary DOUBLE,
        dept STRING
    );

    INSERT INTO TABLE employee_delta VALUES
    (1, '张三', 22000.00, '技术部'),
    (4, '钱八', 14000.00, '人事部');

    -- MERGE：更新已有的，插入新增的
    MERGE INTO employee_acid AS T
    USING employee_delta AS S
    ON T.id = S.id
    WHEN MATCHED THEN UPDATE SET salary = S.salary, dept = S.dept
    WHEN NOT MATCHED THEN INSERT VALUES (S.id, S.name, S.salary, S.dept);

    -- 带条件的MERGE
    MERGE INTO employee_acid AS T
    USING employee_delta AS S
    ON T.id = S.id
    WHEN MATCHED AND S.salary > 20000 THEN UPDATE SET salary = S.salary
    WHEN MATCHED AND S.salary <= 20000 THEN DELETE
    WHEN NOT MATCHED THEN INSERT VALUES (S.id, S.name, S.salary, S.dept);
```

## 6.5 EXPORT / IMPORT
### 6.5.1 EXPORT TABLE TO
语法：
    EXPORT TABLE tablename [PARTITION (part_spec)] TO 'export_path';

说明：
    将表的数据和元数据（DDL）一起导出到指定的HDFS路径。导出的内容可以安全地用于IMPORT恢复，适合数据迁移和备份场景。

举例：
    -- 导出整个表（包含数据和元数据）
    EXPORT TABLE employee TO '/user/hive/backup/employee_20260527';

    -- 导出指定分区
    EXPORT TABLE sales PARTITION (year=2026, month=5)
    TO '/user/hive/backup/sales_202605';

    -- 查看导出的目录结构
    dfs -ls /user/hive/backup/employee_20260527;
    -- 包含 _metadata（元数据） 和 data（数据文件） 目录
```

### 6.5.2 IMPORT TABLE FROM
语法：
    IMPORT TABLE tablename [PARTITION (part_spec)] FROM 'export_path';

说明：
    从EXPORT导出的路径中恢复表。如果表已存在，需要先DROP或使用IMPORT TABLE指定新表名。也可以导入单个分区。

举例：
    -- 在新数据库中导入整个表
    CREATE DATABASE backup_db;
    USE backup_db;
    IMPORT TABLE employee FROM '/user/hive/backup/employee_20260527';

    -- 以新表名导入
    IMPORT TABLE employee_restored FROM '/user/hive/backup/employee_20260527';

    -- 导入指定分区到已有表
    IMPORT TABLE sales PARTITION (year=2026, month=5)
    FROM '/user/hive/backup/sales_202605';

    -- 完整的数据迁移流程
    -- 源集群：导出
    EXPORT TABLE employee TO '/user/hive/migrate/employee';
    -- 将 /user/hive/migrate/employee 从源集群复制到目标集群（使用distcp）
    -- 目标集群：导入
    IMPORT TABLE employee FROM '/user/hive/migrate/employee';
```

## 6.6 TRUNCATE
### 6.6.1 TRUNCATE TABLE
语法：
    TRUNCATE TABLE tablename [PARTITION (part_spec)];

说明：
    清空表中的所有数据（或指定分区中的数据）。TRUNCATE仅删除数据文件，不删除元数据，比DELETE FROM（无WHERE）更快，且不产生事务日志。对于非ACID表同样适用。

举例：
    -- 创建测试表
    CREATE TABLE test_data (id INT, name STRING);
    INSERT INTO TABLE test_data VALUES (1, 'a'), (2, 'b'), (3, 'c');

    -- 清空整个表
    TRUNCATE TABLE test_data;

    -- 验证：表结构还在，数据已清空
    SELECT * FROM test_data;
    -- （无结果返回）
    DESC test_data;
    -- id   int
    -- name string
```

### 6.6.2 TRUNCATE 分区
语法：
    TRUNCATE TABLE tablename PARTITION (partcol1=val1, partcol2=val2 ...);

说明：
    只清空指定分区的数据，其他分区不受影响。支持一次清空多个分区。

举例：
    -- 创建分区表
    CREATE TABLE logs_part (
        user_id INT,
        action STRING
    )
    PARTITIONED BY (dt STRING);

    -- 插入数据到多个分区
    INSERT INTO logs_part PARTITION (dt='2026-05-26') VALUES (1, 'login'), (2, 'logout');
    INSERT INTO logs_part PARTITION (dt='2026-05-27') VALUES (3, 'login'), (4, 'search');

    -- 清空指定分区的数据
    TRUNCATE TABLE logs_part PARTITION (dt='2026-05-26');

    -- 验证：仅 2026-05-26 分区数据被清空
    SELECT * FROM logs_part;
    -- +----------+---------+------------+
    -- | user_id  | action  | dt         |
    -- +----------+---------+------------+
    -- | 3        | login   | 2026-05-27 |
    -- | 4        | search  | 2026-05-27 |
    -- +----------+---------+------------+
```