<!-- TOC  -->
- [1. SELECT 基本语法(Basic SELECT Syntax)](#1-select-基本语法basic-select-syntax)
    - [1.1 SELECT ... FROM](#11-select--from)
    - [1.2 列别名(AS)](#12-列别名as)
    - [1.3 DISTINCT](#13-distinct)
    - [1.4 SELECT 表达式](#14-select-表达式)
- [2. JOIN 连接(Joins)](#2-join-连接joins)
    - [2.1 INNER JOIN](#21-inner-join)
    - [2.2 LEFT OUTER JOIN](#22-left-outer-join)
    - [2.3 RIGHT OUTER JOIN](#23-right-outer-join)
    - [2.4 FULL OUTER JOIN](#24-full-outer-join)
    - [2.5 CROSS JOIN](#25-cross-join)
    - [2.6 LEFT SEMI JOIN](#26-left-semi-join)
    - [2.7 MAPJOIN](#27-mapjoin)
    - [2.8 JOIN 条件: ON vs WHERE](#28-join-条件-on-vs-where)
    - [2.9 多表 JOIN](#29-多表-join)
- [3. 子查询(Subqueries)](#3-子查询subqueries)
    - [3.1 WHERE 子句中的子查询](#31-where-子句中的子查询)
    - [3.2 FROM 子句中的子查询](#32-from-子句中的子查询)
    - [3.3 关联子查询 vs 非关联子查询](#33-关联子查询-vs-非关联子查询)
- [4. CTE 公共表表达式(WITH Clause)](#4-cte-公共表表达式with-clause)
    - [4.1 基本 WITH 语法](#41-基本-with-语法)
    - [4.2 多个 CTE](#42-多个-cte)
    - [4.3 CTE 嵌套引用](#43-cte-嵌套引用)
    - [4.4 CTE vs 子查询对比](#44-cte-vs-子查询对比)
- [5. UNION / UNION ALL](#5-union--union-all)
    - [5.1 UNION（去重）](#51-union去重)
    - [5.2 UNION ALL（不去重）](#52-union-all不去重)
    - [5.3 列对齐规则](#53-列对齐规则)
- [6. 排序(Sorting)](#6-排序sorting)
    - [6.1 ORDER BY（全局排序）](#61-order-by全局排序)
    - [6.2 SORT BY（Reducer 内排序）](#62-sort-byreducer-内排序)
    - [6.3 DISTRIBUTE BY（分区）](#63-distribute-by分区)
    - [6.4 CLUSTER BY](#64-cluster-by)
    - [6.5 排序对比和适用场景](#65-排序对比和适用场景)
- [7. GROUP BY 和 HAVING](#7-group-by-和-having)
    - [7.1 基本分组聚合](#71-基本分组聚合)
    - [7.2 多列分组](#72-多列分组)
    - [7.3 HAVING 过滤](#73-having-过滤)
    - [7.4 GROUPING SETS](#74-grouping-sets)
    - [7.5 ROLLUP](#75-rollup)
    - [7.6 CUBE](#76-cube)
- [8. LATERAL VIEW](#8-lateral-view)
    - [8.1 与 UDTF 配合使用](#81-与-udtf-配合使用)
    - [8.2 多个 LATERAL VIEW](#82-多个-lateral-view)
    - [8.3 LATERAL VIEW OUTER](#83-lateral-view-outer)
- [9. TABLESAMPLE 抽样](#9-tablesample-抽样)
    - [9.1 TABLESAMPLE(n PERCENT)](#91-tablesamplen-percent)
    - [9.2 TABLESAMPLE(n ROWS)](#92-tablesamplen-rows)
    - [9.3 TABLESAMPLE(BUCKET x OUT OF y)](#93-tablesamplebucket-x-out-of-y)
- [10. LIMIT](#10-limit)
    - [10.1 基本 LIMIT](#101-基本-limit)
    - [10.2 LIMIT 与 ORDER BY 配合](#102-limit-与-order-by-配合)
<!-- /TOC -->
# 1. SELECT 基本语法(Basic SELECT Syntax)
## 1.1 SELECT ... FROM
```sql
语法：
    SELECT [ALL | DISTINCT] select_expr [, select_expr ...]
    FROM table_reference
    [WHERE where_condition]
    [GROUP BY col_list]
    [HAVING having_condition]
    [ORDER BY col_list [ASC | DESC]]
    [LIMIT [offset,] rows]

说明：
    SELECT 语句用于从表中查询数据。各子句的逻辑执行顺序为：
    FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT

举例：
    SELECT id, name, age
    FROM users
    WHERE age >= 18
    ORDER BY age DESC
    LIMIT 10;
```
## 1.2 列别名(AS)
```sql
语法：
    SELECT column_name AS alias_name [, ...]
说明：
    AS 关键字可为列或表达式指定别名，AS 可以省略。别名在 WHERE 子句中不可用，
    但在 ORDER BY、GROUP BY 和 HAVING 中可用。

举例：
    SELECT
        id AS user_id,
        name AS user_name,
        salary * 12 annual_salary
    FROM employees
    WHERE salary > 5000
    ORDER BY annual_salary DESC;
    -- 结果：
    -- user_id  user_name  annual_salary
    -- 1003     王五        180000
    -- 1001     张三        144000
```
## 1.3 DISTINCT
```sql
语法：
    SELECT DISTINCT col1 [, col2 ...] FROM table_name
参数类型：
    col: 任意类型
返回值类型：
    与原始列类型相同
说明：
    DISTINCT 用于对结果集去重，作用于所有选择的列组合。ALL 为默认行为，返回所有行。

举例：
    SELECT DISTINCT department, city
    FROM employees
    ORDER BY department;
    -- 结果：
    -- department  city
    -- 技术部      北京
    -- 技术部      上海
    -- 销售部      北京
    -- 销售部      广州
```
## 1.4 SELECT 表达式
```sql
语法：
    SELECT expr1, expr2, ... FROM table_name
说明：
    SELECT 子句中可以使用算术表达式、函数调用、CASE WHEN 等计算字段。

举例：
    SELECT
        id,
        name,
        salary,
        salary * 1.2 AS adjusted_salary,
        CASE WHEN salary >= 10000 THEN '高薪'
             WHEN salary >= 5000 THEN '中等'
             ELSE '普通'
        END AS salary_level,
        concat(name, '-', CAST(id AS STRING)) AS unique_code
    FROM employees;
    -- 结果：
    -- id    name  salary  adjusted_salary  salary_level  unique_code
    -- 1001  张三   12000   14400.0          高薪          张三-1001
    -- 1002  李四   6000    7200.0           中等          李四-1002
    -- 1003  王五   3000    3600.0           普通          王五-1003
```
# 2. JOIN 连接(Joins)
## 2.1 INNER JOIN
```sql
语法：
    SELECT ... FROM table_a [INNER] JOIN table_b ON join_condition
参数类型：
    join_condition: boolean 表达式
返回值类型：
    两表中满足连接条件的行的笛卡尔积
说明：
    返回两表中满足 ON 条件的匹配行。INNER 关键字可省略。只返回匹配成功的行。

举例：
    SELECT
        a.id,
        a.name,
        b.order_id,
        b.amount
    FROM users a
    JOIN orders b ON a.id = b.user_id;
    -- 结果（仅返回有订单的用户）：
    -- id    name  order_id  amount
    -- 1     张三   O001      100.0
    -- 1     张三   O002      200.0
    -- 3     王五   O003      150.0
```
## 2.2 LEFT OUTER JOIN
```sql
语法：
    SELECT ... FROM table_a LEFT [OUTER] JOIN table_b ON join_condition
参数类型：
    join_condition: boolean 表达式
返回值类型：
    左表所有行 + 右表匹配行（不匹配时右表列为 NULL）
说明：
    保留左表所有行。右表无匹配时，右表列填充 NULL。OUTER 关键字可省略。

举例：
    SELECT
        a.id,
        a.name,
        b.order_id,
        b.amount
    FROM users a
    LEFT JOIN orders b ON a.id = b.user_id;
    -- 结果（保留所有用户，无订单的用户 order 列为 NULL）：
    -- id    name  order_id  amount
    -- 1     张三   O001      100.0
    -- 1     张三   O002      200.0
    -- 2     李四   NULL      NULL
    -- 3     王五   O003      150.0
```
## 2.3 RIGHT OUTER JOIN
```sql
语法：
    SELECT ... FROM table_a RIGHT [OUTER] JOIN table_b ON join_condition
参数类型：
    join_condition: boolean 表达式
返回值类型：
    右表所有行 + 左表匹配行（不匹配时左表列为 NULL）
说明：
    保留右表所有行。左表无匹配时，左表列填充 NULL。

举例：
    SELECT
        a.id,
        a.name,
        b.order_id,
        b.amount
    FROM users a
    RIGHT JOIN orders b ON a.id = b.user_id;
    -- 结果（保留所有订单，无对应用户时 user 列为 NULL）：
    -- id    name  order_id  amount
    -- 1     张三   O001      100.0
    -- 1     张三   O002      200.0
    -- 3     王五   O003      150.0
    -- NULL  NULL  O004      300.0
```
## 2.4 FULL OUTER JOIN
```sql
语法：
    SELECT ... FROM table_a FULL [OUTER] JOIN table_b ON join_condition
参数类型：
    join_condition: boolean 表达式
返回值类型：
    两表所有行的并集
说明：
    返回两表中所有行。不匹配的一侧用 NULL 填充。

举例：
    SELECT
        a.id AS user_id,
        a.name,
        b.order_id,
        b.amount
    FROM users a
    FULL JOIN orders b ON a.id = b.user_id;
    -- 结果：
    -- user_id  name  order_id  amount
    -- 1        张三   O001      100.0
    -- 1        张三   O002      200.0
    -- 2        李四   NULL      NULL
    -- 3        王五   O003      150.0
    -- NULL     NULL  O004      300.0
```
## 2.5 CROSS JOIN
```sql
语法：
    SELECT ... FROM table_a CROSS JOIN table_b
说明：
    返回两表的笛卡尔积（所有行的交叉组合）。不带 ON 条件。行数 = 左表行数 × 右表行数。

举例：
    SELECT
        a.name AS user_name,
        b.city
    FROM (SELECT stack(2, '张三', '李四') AS (name)) a
    CROSS JOIN (SELECT stack(3, '北京', '上海', '广州') AS (city)) b;
    -- 结果：
    -- user_name  city
    -- 张三        北京
    -- 张三        上海
    -- 张三        广州
    -- 李四        北京
    -- 李四        上海
    -- 李四        广州
```
## 2.6 LEFT SEMI JOIN
```sql
语法：
    SELECT a.* FROM table_a LEFT SEMI JOIN table_b ON join_condition
说明：
    LEFT SEMI JOIN 等价于 INNER JOIN 后只取左表的列，但效率更高（无需加载右表全部数据）。
    相当于 WHERE a.col IN (SELECT b.col FROM table_b)，但语义上是 JOIN。
    右表列不能出现在 SELECT 和 WHERE 子句中。

举例：
    SELECT a.*
    FROM users a
    LEFT SEMI JOIN orders b ON a.id = b.user_id;
    -- 结果（只返回有订单的用户，且只输出 users 的列）：
    -- id    name  age  city
    -- 1     张三   25   北京
    -- 3     王五   30   广州
```
## 2.7 MAPJOIN
```sql
语法：
    SELECT /*+ MAPJOIN(small_table) */ ... FROM large_table JOIN small_table ON ...
说明：
    MAPJOIN 是一种优化 Hint，将小表加载到内存中，在 Map 阶段完成 JOIN，
    从而避免 Shuffle，大幅提高 JOIN 性能。适用于大表 JOIN 小表的场景。
    支持同时指定多个小表：/*+ MAPJOIN(t1, t2) */

举例：
    SELECT /*+ MAPJOIN(b) */
        a.user_id,
        a.order_amount,
        b.dict_name
    FROM orders a
    JOIN dict_area b ON a.area_code = b.area_code;
```
## 2.8 JOIN 条件: ON vs WHERE
```sql
说明：
    INNER JOIN 中，ON 和 WHERE 效果相同。
    OUTER JOIN 中，ON 决定匹配逻辑，WHERE 对 JOIN 后的结果集进行过滤。
    OUTER JOIN 时，将条件放在 WHERE 中可能会把外连接退化为内连接。

举例：
    SELECT a.id, a.name, b.amount
    FROM users a
    LEFT JOIN orders b ON a.id = b.user_id
    WHERE b.amount > 100;
    -- 结果：NULL 行被过滤，实际效果等同 INNER JOIN
    -- id    name  amount
    -- 1     张三   200.0
    -- 3     王五   150.0

    SELECT a.id, a.name, b.amount
    FROM users a
    LEFT JOIN orders b ON a.id = b.user_id AND b.amount > 100;
    -- 结果：保留所有用户，但只匹配 amount > 100 的订单
    -- id    name  amount
    -- 1     张三   200.0
    -- 2     李四   NULL
    -- 3     王五   150.0
```
## 2.9 多表 JOIN
```sql
语法：
    SELECT ... FROM t1 JOIN t2 ON ... JOIN t3 ON ... [JOIN t4 ON ...]
说明：
    Hive 支持链式 JOIN，多表 JOIN 时需注意连接顺序对性能的影响。
    建议将大表放后面、小表放前面（或使用 MAPJOIN）。

举例：
    SELECT
        u.name,
        o.order_id,
        o.amount,
        p.product_name,
        p.price
    FROM users u
    JOIN orders o ON u.id = o.user_id
    JOIN products p ON o.product_id = p.id
    WHERE o.amount > 100;
    -- 结果：
    -- name  order_id  amount  product_name  price
    -- 张三   O001      200.0   手机          5000.0
    -- 王五   O003      150.0   耳机          200.0
```
# 3. 子查询(Subqueries)
## 3.1 WHERE 子句中的子查询
```sql
说明：
    在 WHERE 子句中使用子查询进行条件筛选，支持 IN、NOT IN、EXISTS、NOT EXISTS
    以及标量子查询（返回单值）。Hive 对 IN 子查询的支持有限，建议优先使用
    LEFT SEMI JOIN 或 EXISTS。

举例：
    SELECT id, name
    FROM users
    WHERE id IN (SELECT user_id FROM orders WHERE amount > 100);
    -- 结果：
    -- id    name
    -- 1     张三
    -- 3     王五

    SELECT a.id, a.name
    FROM users a
    WHERE EXISTS (
        SELECT 1 FROM orders b
        WHERE b.user_id = a.id AND b.amount > 100
    );
    -- 结果：
    -- id    name
    -- 1     张三
    -- 3     王五

    SELECT id, name, salary
    FROM employees
    WHERE salary > (SELECT AVG(salary) FROM employees);
    -- 结果：返回薪资高于平均薪资的员工
```
## 3.2 FROM 子句中的子查询
```sql
语法：
    SELECT ... FROM (SELECT ... FROM table) alias
说明：
    在 FROM 子句中使用的子查询称为派生表(Derived Table)，必须指定别名。
    常用于对中间结果集进行二次聚合或过滤。

举例：
    SELECT
        dept,
        MAX(total_salary) AS max_dept_salary
    FROM (
        SELECT
            department AS dept,
            SUM(salary) AS total_salary
        FROM employees
        GROUP BY department
    ) t
    GROUP BY dept;
    -- 结果：
    -- dept      max_dept_salary
    -- 技术部     35000
    -- 销售部     28000
```
## 3.3 关联子查询 vs 非关联子查询
```sql
说明：
    非关联子查询：子查询独立于外层查询，先执行子查询再执行外层查询。
    关联子查询：子查询引用外层查询的列，对外层每一行都执行一次子查询。
    关联子查询在 Hive 中性能较低，建议使用 JOIN 改写。

举例：
    SELECT a.id, a.name
    FROM employees a
    WHERE a.salary > (
        SELECT AVG(b.salary)
        FROM employees b
        WHERE b.department = a.department
    );
    -- 结果：返回薪资高于本部门平均薪资的员工（关联子查询）

    SELECT a.id, a.name
    FROM employees a
    JOIN (
        SELECT department, AVG(salary) AS avg_salary
        FROM employees
        GROUP BY department
    ) b ON a.department = b.department
    WHERE a.salary > b.avg_salary;
    -- 结果：与上面等价，用 JOIN 改写性能更好
```
# 4. CTE 公共表表达式(WITH Clause)
## 4.1 基本 WITH 语法
```sql
语法：
    WITH cte_name [(col_alias [, ...])] AS (
        SELECT ...
    )
    SELECT ... FROM cte_name ...
说明：
    CTE（Common Table Expression）定义了一个在单个查询中可重复引用的临时结果集。
    提高了可读性和可维护性，在 Hive 中 CTE 会被内联展开。

举例：
    WITH dept_stats AS (
        SELECT
            department,
            COUNT(*) AS emp_count,
            AVG(salary) AS avg_salary
        FROM employees
        GROUP BY department
    )
    SELECT *
    FROM dept_stats
    WHERE avg_salary > 6000;
    -- 结果：
    -- department  emp_count  avg_salary
    -- 技术部       5          8500.0
```
## 4.2 多个 CTE
```sql
语法：
    WITH
        cte1 AS (SELECT ...),
        cte2 AS (SELECT ...)
    SELECT ... FROM cte1 JOIN cte2 ON ...
说明：
    可以在一个 WITH 子句中定义多个 CTE，用逗号分隔。CTE 之间可以相互引用。

举例：
    WITH
        user_orders AS (
            SELECT user_id, COUNT(*) AS order_cnt, SUM(amount) AS total_amount
            FROM orders
            GROUP BY user_id
        ),
        active_users AS (
            SELECT user_id, order_cnt, total_amount
            FROM user_orders
            WHERE order_cnt >= 2
        )
    SELECT u.name, a.order_cnt, a.total_amount
    FROM users u
    JOIN active_users a ON u.id = a.user_id;
    -- 结果：
    -- name  order_cnt  total_amount
    -- 张三   2          300.0
```
## 4.3 CTE 嵌套引用
```sql
说明：
    后定义的 CTE 可以引用前面定义的 CTE，形成链式依赖。但不能前向引用
    （不能引用尚未定义的 CTE）。

举例：
    WITH
        raw_data AS (
            SELECT * FROM orders WHERE dt >= '2024-01-01'
        ),
        daily_stats AS (
            SELECT dt, COUNT(*) AS order_cnt, SUM(amount) AS total_amount
            FROM raw_data
            GROUP BY dt
        ),
        weekly_avg AS (
            SELECT
                WEEKOFYEAR(dt) AS week_num,
                AVG(total_amount) AS avg_daily_amount
            FROM daily_stats
            GROUP BY WEEKOFYEAR(dt)
        )
    SELECT *
    FROM weekly_avg
    ORDER BY week_num;
```
## 4.4 CTE vs 子查询对比
```sql
说明：
    CTE 优势：
    1. 可读性更好，逻辑清晰，从顶向下阅读
    2. 可被多次引用，避免重复编写相同的子查询
    3. 支持递归（Hive 不完全支持递归 CTE）
    
    子查询优势：
    1. 简单场景编写更快捷
    2. 单次使用的简单筛选无需单独定义 CTE

举例：
    SELECT *
    FROM (
        SELECT department, COUNT(*) AS cnt
        FROM (
            SELECT * FROM employees WHERE salary > 5000
        ) t1
        GROUP BY department
    ) t2
    WHERE cnt >= 2;

    WITH high_salary_emp AS (
        SELECT * FROM employees WHERE salary > 5000
    ),
    dept_agg AS (
        SELECT department, COUNT(*) AS cnt
        FROM high_salary_emp
        GROUP BY department
    )
    SELECT * FROM dept_agg WHERE cnt >= 2;
    -- 两者等价，CTE 写法更清晰
```
# 5. UNION / UNION ALL
## 5.1 UNION（去重）
```sql
语法：
    SELECT col1, col2 ... FROM table_a
    UNION
    SELECT col1, col2 ... FROM table_b
说明：
    合并两个或多个 SELECT 的结果，自动去除重复行。UNION 会触发额外的去重操作，
    性能低于 UNION ALL。

举例：
    SELECT id, name FROM employees_dept1
    UNION
    SELECT id, name FROM employees_dept2
    ORDER BY id;
    -- 结果：合并两个部门员工并去重
    -- id    name
    -- 1     张三
    -- 2     李四
    -- 3     王五
```
## 5.2 UNION ALL（不去重）
```sql
语法：
    SELECT col1, col2 ... FROM table_a
    UNION ALL
    SELECT col1, col2 ... FROM table_b
说明：
    合并两个或多个 SELECT 的结果，保留所有行（包括重复行）。
    性能优于 UNION，仅在确实需要去重时使用 UNION。

举例：
    SELECT dt, SUM(amount) AS daily_amount FROM orders_2024_01
    UNION ALL
    SELECT dt, SUM(amount) AS daily_amount FROM orders_2024_02
    UNION ALL
    SELECT dt, SUM(amount) AS daily_amount FROM orders_2024_03
    ORDER BY dt;
    -- 结果：合并三个月的每日汇总数据
```
## 5.3 列对齐规则
```sql
说明：
    UNION / UNION ALL 要求：
    1. 各 SELECT 语句的列数必须相同
    2. 对应列的数据类型必须兼容（可隐式转换）
    3. 最终结果的列名取第一个 SELECT 的列名
    4. ORDER BY 只能出现在最后，对整个合并结果排序

举例：
    SELECT id, name, 'A' AS source FROM table_a
    UNION ALL
    SELECT user_id AS id, user_name AS name, 'B' AS source FROM table_b
    ORDER BY id;
    -- 结果列名为：id, name, source（取自第一个 SELECT）
```
# 6. 排序(Sorting)
## 6.1 ORDER BY（全局排序）
```sql
语法：
    SELECT ... FROM table ORDER BY col [ASC | DESC] [NULLS FIRST | NULLS LAST]
说明：
    对结果集进行全局排序，所有数据汇聚到一个 Reducer。排序字段可以是列名、
    别名或位置序号。ASC 为默认升序，NULL 在升序时排最前。

举例：
    SELECT id, name, salary
    FROM employees
    ORDER BY salary DESC NULLS LAST
    LIMIT 5;
    -- 结果：按薪资降序排列，NULL 值排在最后，取前5条
```
## 6.2 SORT BY（Reducer 内排序）
```sql
语法：
    SELECT ... FROM table SORT BY col [ASC | DESC]
说明：
    SORT BY 在每个 Reducer 内部排序，不保证全局有序。适用于仅需局部有序的场景，
    性能优于 ORDER BY（每个 Reducer 独立排序，不需要全局 Shuffle）。

举例：
    SELECT id, name, salary
    FROM employees
    SORT BY salary DESC
    LIMIT 100;
    -- 结果：每个 Reducer 内部按薪资降序排列，不同 Reducer 输出间不保证顺序
```
## 6.3 DISTRIBUTE BY（分区）
```sql
语法：
    SELECT ... FROM table DISTRIBUTE BY col
说明：
    DISTRIBUTE BY 控制数据如何分配到不同 Reducer（按指定列的 Hash 值分区）。
    通常与 SORT BY 配合使用，效果类似 GROUP BY + ORDER BY。

举例：
    SELECT department, id, name, salary
    FROM employees
    DISTRIBUTE BY department
    SORT BY department, salary DESC;
    -- 结果：按部门分区，每个部门内按薪资降序排列
    -- 技术部  1003  张三  12000
    -- 技术部  1005  赵六  9000
    -- 销售部  1001  李四  8000
    -- 销售部  1002  王五  6000
```
## 6.4 CLUSTER BY
```sql
语法：
    SELECT ... FROM table CLUSTER BY col
说明：
    CLUSTER BY 等价于 DISTRIBUTE BY col + SORT BY col。
    当分桶字段和排序字段相同时使用，语法更简洁。

举例：
    SELECT department, COUNT(*) AS cnt
    FROM employees
    CLUSTER BY department;
    -- 等效于：
    SELECT department, COUNT(*) AS cnt
    FROM employees
    DISTRIBUTE BY department
    SORT BY department;
```
## 6.5 排序对比和适用场景
```sql
说明：
    ORDER BY：全局排序，一个 Reducer，数据量大时性能差。适用最终输出需要全局有序的场景。
    SORT BY：Reducer 内排序，多个 Reducer 并行，性能好。适用局部有序即可的中间结果。
    DISTRIBUTE BY：控制分桶，不排序。通常与 SORT BY 配合使用。
    CLUSTER BY：DISTRIBUTE BY + SORT BY 的简写，要求分桶和排序使用同一列。

    ORDER BY 与 SORT BY 不可同时使用。DISTRIBUTE BY 必须在 SORT BY 之前。
```
# 7. GROUP BY 和 HAVING
## 7.1 基本分组聚合
```sql
语法：
    SELECT col, agg_func(col) FROM table GROUP BY col
说明：
    将数据按指定列分为若干组，对每组使用聚合函数计算。
    SELECT 中的列必须出现在 GROUP BY 中或是聚合函数的结果。

举例：
    SELECT
        department,
        COUNT(*) AS emp_count,
        SUM(salary) AS total_salary,
        AVG(salary) AS avg_salary
    FROM employees
    GROUP BY department;
    -- 结果：
    -- department  emp_count  total_salary  avg_salary
    -- 技术部       3          35000         11666.67
    -- 销售部       2          18000         9000.0
    -- NULL       1          5000          5000.0
```
## 7.2 多列分组
```sql
语法：
    SELECT col1, col2, agg_func(col) FROM table GROUP BY col1, col2
说明：
    按多列组合进行分组，分组键的组合唯一确定一个分组。

举例：
    SELECT
        department,
        gender,
        COUNT(*) AS cnt,
        AVG(salary) AS avg_salary
    FROM employees
    GROUP BY department, gender
    ORDER BY department, gender;
    -- 结果：
    -- department  gender  cnt  avg_salary
    -- 技术部       F       2    10000.0
    -- 技术部       M       1    15000.0
    -- 销售部       F       1    6000.0
    -- 销售部       M       1    12000.0
```
## 7.3 HAVING 过滤
```sql
语法：
    SELECT ... FROM table GROUP BY col HAVING condition
说明：
    HAVING 对分组后的结果进行过滤，作用于聚合函数的结果。
    与 WHERE 的区别：WHERE 在分组前对原始行过滤，HAVING 在分组后对聚合结果过滤。

举例：
    SELECT
        department,
        COUNT(*) AS emp_count,
        AVG(salary) AS avg_salary
    FROM employees
    WHERE salary > 3000
    GROUP BY department
    HAVING COUNT(*) >= 2 AND AVG(salary) > 8000;
    -- 结果：先过滤薪资 > 3000 的员工，再按部门分组，最后筛选人数 >= 2 且平均薪资 > 8000 的部门
    -- department  emp_count  avg_salary
    -- 技术部       3          11666.67
```
## 7.4 GROUPING SETS
```sql
语法：
    SELECT col1, col2, agg_func(col) FROM table
    GROUP BY col1, col2 GROUPING SETS ((col1), (col2), (col1, col2), ())
说明：
    GROUPING SETS 支持在一次查询中对多个分组维度进行聚合，等价于多个 GROUP BY
    的 UNION ALL。空括号 () 表示全局汇总。未参与分组的列显示为 NULL。

举例：
    SELECT
        department,
        gender,
        COUNT(*) AS cnt,
        SUM(salary) AS total_salary
    FROM employees
    GROUP BY department, gender
    GROUPING SETS ((department), (gender), (department, gender), ());
    -- 结果：
    -- department  gender  cnt  total_salary
    -- 技术部       NULL    3    35000
    -- 销售部       NULL    2    18000
    -- NULL        F       3    26000
    -- NULL        M       2    27000
    -- 技术部       F       2    20000
    -- 技术部       M       1    15000
    -- 销售部       F       1    6000
    -- 销售部       M       1    12000
    -- NULL        NULL    5    53000
```
## 7.5 ROLLUP
```sql
语法：
    SELECT col1, col2, col3, agg_func(col) FROM table
    GROUP BY col1, col2, col3 WITH ROLLUP
说明：
    ROLLUP 是 GROUPING SETS 的语法糖，按层级从右向左逐步上卷。
    对 GROUP BY (a, b, c) WITH ROLLUP 等价于：
    GROUPING SETS ((a,b,c), (a,b), (a), ())。

举例：
    SELECT
        department,
        city,
        SUM(salary) AS total_salary
    FROM employees
    GROUP BY department, city WITH ROLLUP;
    -- 结果：
    -- department  city   total_salary
    -- 技术部       北京    20000
    -- 技术部       上海    15000
    -- 技术部       NULL   35000
    -- 销售部       广州    18000
    -- 销售部       NULL   18000
    -- NULL        NULL   53000
```
## 7.6 CUBE
```sql
语法：
    SELECT col1, col2, agg_func(col) FROM table
    GROUP BY col1, col2 WITH CUBE
说明：
    CUBE 生成所有维度组合的聚合结果。
    对 GROUP BY (a, b) WITH CUBE 等价于：
    GROUPING SETS ((a,b), (a), (b), ())。

举例：
    SELECT
        department,
        gender,
        SUM(salary) AS total_salary
    FROM employees
    GROUP BY department, gender WITH CUBE;
    -- 结果：
    -- department  gender  total_salary
    -- 技术部       F       20000
    -- 技术部       M       15000
    -- 技术部       NULL    35000
    -- 销售部       F       6000
    -- 销售部       M       12000
    -- 销售部       NULL    18000
    -- NULL        F       26000
    -- NULL        M       27000
    -- NULL        NULL    53000
```
# 8. LATERAL VIEW
## 8.1 与 UDTF 配合使用
```sql
语法：
    SELECT ... FROM table
    LATERAL VIEW udtf_func(column) alias AS col_name [, ...]
说明：
    LATERAL VIEW 与 UDTF（如 EXPLODE、posexplode、inline 等）配合使用，
    将一行中的复杂类型（ARRAY、MAP）展开为多行。

举例：
    SELECT name, hobby
    FROM (
        SELECT '张三' AS name, ARRAY('篮球', '游泳', '读书') AS hobbies
        UNION ALL
        SELECT '李四', ARRAY('编程', '跑步')
    ) t
    LATERAL VIEW EXPLODE(hobbies) h AS hobby;
    -- 结果：
    -- name  hobby
    -- 张三   篮球
    -- 张三   游泳
    -- 张三   读书
    -- 李四   编程
    -- 李四   跑步
```
## 8.2 多个 LATERAL VIEW
```sql
语法：
    SELECT ... FROM table
    LATERAL VIEW EXPLODE(arr_col1) alias1 AS col1
    LATERAL VIEW EXPLODE(arr_col2) alias2 AS col2
说明：
    可以串联多个 LATERAL VIEW，后面的 LATERAL VIEW 可以引用前面的 LATERAL VIEW
    生成的结果。

举例：
    SELECT name, skill, tool
    FROM (
        SELECT '张三' AS name, ARRAY('Java', 'Python') AS skills, ARRAY('Git', 'Docker') AS tools
    ) t
    LATERAL VIEW EXPLODE(skills) s AS skill
    LATERAL VIEW EXPLODE(tools) tl AS tool;
    -- 结果：
    -- name  skill    tool
    -- 张三   Java     Git
    -- 张三   Java     Docker
    -- 张三   Python   Git
    -- 张三   Python   Docker
```
## 8.3 LATERAL VIEW OUTER
```sql
语法：
    SELECT ... FROM table
    LATERAL VIEW OUTER EXPLODE(col) alias AS col_name
说明：
    LATERAL VIEW 在数组/MAP 为空或 NULL 时不会产生任何行。
    LATERAL VIEW OUTER 即使展开列为空也会保留原始行，展开列填充 NULL。

举例：
    SELECT name, hobby
    FROM (
        SELECT '张三' AS name, ARRAY('篮球', '游泳') AS hobbies
        UNION ALL
        SELECT '李四', ARRAY() AS hobbies
        UNION ALL
        SELECT '王五', NULL AS hobbies
    ) t
    LATERAL VIEW OUTER EXPLODE(hobbies) h AS hobby;
    -- 结果：
    -- name  hobby
    -- 张三   篮球
    -- 张三   游泳
    -- 李四   NULL
    -- 王五   NULL
```
# 9. TABLESAMPLE 抽样
## 9.1 TABLESAMPLE(n PERCENT)
```sql
语法：
    SELECT ... FROM table TABLESAMPLE(n PERCENT) ...
参数类型：
    n: DOUBLE，百分比值，0~100
说明：
    按百分比随机抽样，每个 HDFS block 上的数据按 n% 比例抽取。
    并非精确按全局行数的百分比抽取，而是按数据块的百分比。

举例：
    SELECT * FROM orders TABLESAMPLE(10 PERCENT);
    -- 结果：大约返回 10% 的数据
```
## 9.2 TABLESAMPLE(n ROWS)
```sql
语法：
    SELECT ... FROM table TABLESAMPLE(n ROWS) ...
参数类型：
    n: INT，行数
说明：
    按行数随机抽样，从每个 InputSplit 中抽取 n 行。不是精确返回 n 行，
    而是每个 Split 返回 n 行。

举例：
    SELECT * FROM orders TABLESAMPLE(100 ROWS);
    -- 结果：每个 Split 返回约 100 行数据
```
## 9.3 TABLESAMPLE(BUCKET x OUT OF y)
```sql
语法：
    SELECT ... FROM table TABLESAMPLE(BUCKET x OUT OF y [ON col]) ...
参数类型：
    x, y: INT，分桶编号和总分桶数
    col: 分桶列（可选，默认使用表格自身分桶列）
说明：
    按分桶抽样，将数据按分桶列 Hash 分为 y 份，取第 x 份。
    要求 y 的值为表格创建时 CLUSTERED BY 桶数的倍数或约数。

举例：
    CREATE TABLE orders_bucketed (
        order_id STRING,
        amount DOUBLE
    )
    CLUSTERED BY (order_id) INTO 16 BUCKETS;

    SELECT * FROM orders_bucketed TABLESAMPLE(BUCKET 3 OUT OF 8 ON order_id);
    -- 结果：将数据按 order_id 的 Hash 值分为 8 份，取第 3 份
```
# 10. LIMIT
## 10.1 基本 LIMIT
```sql
语法：
    SELECT ... FROM table LIMIT n
参数类型：
    n: INT
说明：
    限制返回的行数。Hive 执行 LIMIT 时会优化为只读取少量数据，
    不进行全表扫描（如无 ORDER BY 的情况）。LIMIT 可以带一个 offset 参数：
    LIMIT [offset,] rows，实现分页效果。

举例：
    SELECT * FROM employees LIMIT 5;
    -- 结果：返回前5行

    SELECT * FROM employees LIMIT 10, 5;
    -- 结果：跳过前10行，返回5行（第11-15行）
```
## 10.2 LIMIT 与 ORDER BY 配合
```sql
语法：
    SELECT ... FROM table ORDER BY col LIMIT n
说明：
    ORDER BY + LIMIT 组合可获取排名前N/后N的数据。注意 ORDER BY 要求全局排序，
    当数据量较大时可能成为性能瓶颈。

举例：
    SELECT id, name, salary
    FROM employees
    ORDER BY salary DESC
    LIMIT 3;
    -- 结果：返回薪资最高的前3名员工
    -- id    name  salary
    -- 1003  张三   15000
    -- 1001  李四   12000
    -- 1005  赵六   10000

    SELECT dt, SUM(amount) AS daily_amount
    FROM orders
    GROUP BY dt
    ORDER BY daily_amount ASC
    LIMIT 1;
    -- 结果：返回订单金额最低的一天